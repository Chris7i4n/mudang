# gumiho-lsp

Standalone Rust crate for managing Language Server Protocol clients.

## Development

```bash
make check
make test
make fmt
```

Run the smoke example against `rust-analyzer`:

```bash
cargo run --example lsp_smoke
```
