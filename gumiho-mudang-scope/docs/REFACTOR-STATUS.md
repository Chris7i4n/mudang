# Refactor Status

Live state of `ARCHITECTURAL-REFACTOR.md`. Append-only log; the snapshot tables at the top reflect the current state.

Until the refactor closes (Phase E acceptance — see `ARCHITECTURAL-REFACTOR.md` "Acceptance for the refactor as a whole"), feature work is paused.

## Phases

| Phase | Moves | Status | Started | Shipped | Notes |
|---|---|---|---|---|---|
| A — Schema and storage | R0, R1 | shipped | 2026-05-11 | 2026-05-11 | first batch; everything else depends on it |
| B — Plugin layer | R2, R3, R4, R7, R9, R11, R12 | shipped | 2026-05-11 | 2026-05-12 | three sprints (0002 R7+R4, 0003 R2+R3, 0004 R9+R11+R12) merged onto integration branch refactor/phase-b; phase-close commit demonstrates every Phase B acceptance bullet; phase-scope codex review (gpt-5.5, high reasoning override) clean |
| C — Framework layer | R5 | shipped | 2026-05-12 | 2026-05-12 | single-sprint phase; sprint 0005 closes Phase C atomically in the same commit; `just gate-refactor` runs 12 active CI gates green |
| D — Output and audit | R8, R10 | shipped | 2026-05-12 | 2026-05-12 | two sprints (sprint 0006 R10; sprint 0007 R8) onto `refactor/phase-d` integration branch (precedent: Phase B); atomic phase close — sprint 0007 close commit `1c4a011` flips R8 + Phase D to shipped on the sprint branch; `refactor/phase-d` then merges to `main`; mid-sprint scope discovery 2026-05-12 split the original single-sprint plan into two |
| E — Test harness and charter close | R6 + charter sweep (no R-move) | shipped | 2026-05-12 | 2026-05-12 | two sprints — 0008 (R6 harness, commit 6f22780) then 0009 (charter sweep + shim retirement). Sprint 0009 atomic close flips Phase E + refactor-as-a-whole to `shipped` in the same commit. Two codex passes clean: sprint-scope (round 1 medium: 1 P3 closed, round 2 medium: clean) + full-refactor-scope (high reasoning override against pre-refactor baseline 9dc02c7: clean) |

## Moves

| ID | Title | Phase | Status | Commit | Date | Notes |
|---|---|---|---|---|---|---|
| R0 | Schema closures | A | shipped | ac8dcb3 | 2026-05-11 | adds confidence/status/producer/pattern_id/args_text, surrogate edge_id PK, contains + 30 domain edge kinds (R0 baseline 13 + Tier 1 5 + Tier 2 5 + Tier 3 7; final whitelist 38), `goroutine_spawn` renamed to `green_thread_spawn`, 4-kind concurrency split, 3 new symbol kinds, skipped_ranges. No in-place migration — old indexes are wiped and re-indexed |
| R1 | Typed edge insertion API | A | shipped | 1192346 | 2026-05-11 | seals Edge; EdgeBuilder is sole producer of RawEdge; no .status() at extraction; Phase A trivial resolver stub in scope-graph::resolver (retired by R3) |
| R2 | LanguagePlugin output type closure | B | shipped | 7b029e9 | 2026-05-12 | plugin returns `RawCaptures { matches, metadata, skipped_ranges }`; the 7 per-language extractors live in `scope-core/src/extract/<lang>.rs`; `make_edge(from, to, kind, pattern_id, file_path, line)` is the sole construction site; no `EdgeKind` decision lives outside the extractor surface; `extract_edges(lang, file_path, &RawCaptures) → Vec<RawEdge>` is the typed dispatch; `legacy.<kind>` placeholder + HashMap signature compat shims retired in sprint 0003 chunk 7 |
| R3 | Pipeline ordering via type-state | B | shipped | 563f9d7 | 2026-05-12 | pipeline `extract → resolve → write` encoded as `RawCaptures → RawEdge → InsertableEdge → Graph`; resolver is sole producer of `status` (`Resolved` / `Ambiguous` / `Dangling`); ambiguous emits one row per candidate (R3 acceptance bullet 3); `confidence` preserved verbatim through resolution (acceptance bullet 4); `Graph::insert_resolved_edges` accepts only `&[InsertableEdge]`; `InsertableEdge::new` + struct literal callable only inside `scope_graph::resolve`; Insertable typestate CI gate active via three compile-fail fixtures in `scope-graph/tests/compile_fail/typestate/` |
| R4 | WorkspaceContext typed access (split) | B | shipped | 7fc7d28 | 2026-05-12 | `LanguageWorkspaceContext` (Sync+Send trait in `scope-core/src/workspace_context.rs`) exposes ONLY framework-version accessor + symbol-table view; no `edition` / `target` / `python_requires` / `go_directive` / `tsconfig_target` / `framework_versions` accessors (`audit_context_shape.sh` pins this); `FrameworkWorkspaceContext` ships `pub(crate)` per the sprint 0002 amendment (widens to `pub` in sprint 0005 alongside the first `FrameworkPlugin` impl); cargo / go / python / npm / etc. manifest readers live workspace-side (`scope-core/src/workspace/`), never on the plugin surface (`scripts/grep_no_fs.sh` pins this) |
| R7 | Indexer-level dispatch enforcement | B | shipped | 49ff313 | 2026-05-12 | compile-time `const fn dispatch_extension(&str) -> Option<LanguageId>` + `const fn dispatch_shebang(&str) -> Option<LanguageId>` generated by `register_languages!` macro in `scope-core/src/languages/dispatch.rs`; `const _: () = assert_no_extension_overlap(...)` panics at build time if two languages claim the same extension; `parser.rs::detect_language` removed; the indexer is the only `dispatch_*` consumer; per the A.4 trait-collapse amendment (sprint 0002), there is no `LanguagePlugin` trait and no `*Plugin` unit structs — `LanguageId` enum with exhaustive `match self` arms is the entire plugin surface; CI gate `grep_dispatch.sh` pins no content-reader in `languages/` |
| R5 | FrameworkPlugin operates on Symbols and Edges | C | shipped | 4e7f5ed | 2026-05-12 | `FrameworkPlugin` trait + `Detection` / `DetectedVersion` / `ResolvedVersion` / `UnknownVersionPolicy` / `Pattern` types in `scope-core/src/frameworks/`; cross-language pre-filter in `dispatch::run_frameworks` (edge keep-rule `from_id ∈ kept` only — codex round-1 P2 closed the looser OR form); `FrameworkWorkspaceContext` widened `pub(crate)`→`pub` in same commit as first impl per sprint plan ambiguity #3 (mandatory); synthetic test framework at `scope-core/tests/synthetic_framework/` exercises every R5 acceptance bullet (11 integration tests + 9 dispatch unit tests); CI gates `ci-no-framework-scm` + `ci-patterns` active; `audit_trait_shape.sh` extended to scan `frameworks/` so future `FrameworkPlugin` impls cannot regress negative trait shape; indexer dispatch seam documented as a comment in `scope-index::indexer` (production wiring lands when first real framework adopts post-refactor); codex sprint review (round 1 medium gpt-5.5: 1 P2 closed; round 2 high gpt-5.3-codex: clean) |
| R6 | Malformed-source test harness | E | shipped | 6f22780 | 2026-05-12 | sprint 0008 atomic close: 35-fixture hand-crafted-synthetic corpus at `scope-core/tests/fixtures/malformed/<language_slug>/<case>/` (5 × 7 languages); `malformed/README.md` documents protocol (hand-crafted-synthetic, one failure per fixture, parseable-prefix preamble convention, per-language category table); `scope-core/tests/malformed_sources.rs` is the integration harness with two `#[test]` fns (`floor_5_fixtures_per_language` + `every_fixture_satisfies_r6_acceptance`); 35 `insta` snapshot files at `scope-core/tests/snapshots/malformed_sources/<lang>__<case>.snap` pin recorded reason + range per fixture; indexer-side wiring confirmed end-to-end (`scan_tree_sitter_errors` + `LanguageId::plugin_skipped_ranges` → `CodeParser::collect_skipped_ranges` → `ParsedFile.skipped_ranges` → `file_hashes.skipped_ranges`); reason families spec corrected (`tree_sitter_error:syntax_error`, `tree_sitter_error:missing_node`, `plugin_skip:<plugin>:<rationale>` — aspirational `unrecoverable_node` removed); CI gate flipped active in `CI-GATES.md` + `just test-malformed` recipe + `gate-refactor` aggregator extended (14 → 15 gates, all green); codex review (sprint scope) clean. **Phase E row stays `in-progress`** — sprint 0009 (charter sweep) closes Phase E and the refactor as a whole per the renumbered Phase E layout. Close commit SHA recorded in the matching log entry below |
| R8 | Confidence audit subcommand | D | shipped | 1c4a011 | 2026-05-12 | sprint 0007 atomic close: `scope audit confidence` subcommand wired end-to-end across chunks 1-8 — reference fixture corpus scaffold + clap surface + precision-only disclaimer + sampling engine (`N = 30` default per `(kind, confidence)` cell, `--sample-size N` / `--seed N` overrides, deterministic xorshift64) + two-phase labelling (`--emit-sample` / `--label`) over the AUDIT-LABEL-SCHEMA.md JSONL contract (schema_version `"1"`, plug-point for any external labeller) + hard mechanical SHA-256 drift gate via `Graph::check_audit_freshness` (no `--allow-drift` escape per auditor immutability rule) + precision report (default `--format json` with `schema_version` / `disclaimer` / `sample_schema_doc` / `report[]`; `--format tsv` with `#`-prefixed disclaimer + schema-doc preamble) + tier gate (`high ≥ 95%`, `medium ≥ 70%`, `low` no minimum — every offender surfaced, unknown tier hard-fails) + `just audit-confidence` recipe + CI-GATES row 29 flipped `planned (Phase D)` → `active (sprint 0007 — 2026-05-12)`; null-label rejection enforces the honesty principle at the parse boundary (Priority 2 alignment). Continuous re-audit cycle (committed labelled samples + cross-reindex join key + precision-drift detection over time) lives in POST-REFACTOR-PLAN.md § Priority 1 — Self-correction cycle. Audit module extraction from `gumiho-mudang-cli/src/commands/audit.rs` into a `scope-audit` sub-crate queued at POST-REFACTOR-PLAN.md § Priority 3 — Layering audit |
| R9 | Immutable source guarantee | B | shipped | d76cf7c | 2026-05-12 | `scripts/audit_immutable.sh` (gate `ci-immutable`) greps `scope-core/src/{languages,extract,parser.rs}` for `&[lifetime]? mut (str / String / tree_sitter::Tree / Tree / (path::)*Source[A-Za-z0-9_]*)`; allowlist tag `// scope:audit-allow mutable-source — <rationale>`. Six rounds of codex sprint-scope review closed nine grep-pattern bypasses (lifetime-qualified, bare Source, qualified Source paths, etc.) before sprint-close |
| R10 | Typed output schema | D | shipped | 25a5e9c | 2026-05-12 | sprint 0006 strict reading delivered: every `--json` CLI path emits `JsonOutput<TypedView>` with zero `serde_json::json!()` macro sites and zero `serde_json::Value` ad-hoc trees in `gumiho-mudang-cli/src/{commands,output}`; every plain-text renderer is `impl fmt::Display` on a typed view struct in `output/formatter.rs` (zero `pub fn print_*` free functions; zero residual `println!` / `eprintln!` outside Display bodies); `ci-output-schema` gate active (bans `error/warning/diagnostic/is_valid/lint/correctness` field names); 13 `gate-refactor` gates green; insta snapshots match byte-for-byte after Display conversion (no `.new` files); `EdgeSummary` sum type scaffolded chunk 1 then dropped chunk c (YAGNI — `Reference` / `Dependency` / `ImpactNode` already serialise direct through the envelope; re-introduce when a downstream consumer needs edge-uniform handling) |
| R11 | Macro definition-only by trait shape | B | shipped | d76cf7c | 2026-05-12 | covered by the trait-shape audit's `expand_*` arm (gate `ci-trait-shape`); macro symbols already index under `kind: macro` since R0 (sprint 0001); macro invocations land as `calls.macro` / `calls.macro.scoped` edges to the macro symbol per the per-language extractors (verified in `scope-core/src/extract/rust_lang.rs:88`); the trait-collapse amendment (A.4) means there is no `LanguagePlugin` trait to inspect — the negative shape is enforced over `impl LanguageId` + per-language modules + per-extractor modules instead, with the negative-trait-shape doc block on `id.rs` module header |
| R12 | Type-system-free trait audit + spawn denylist | B | shipped | d76cf7c | 2026-05-12 | three audit scripts active: `scripts/audit_trait_shape.sh` (no `fn (infer_/evaluate_/solve_/narrow_/resolve_overload_/expand_)\w+` in scanned paths), `scripts/audit_no_spawn.sh` (no `Command::new(` / `process::Command` / `std::process::Command` / `process::{...Command...}` / `Command as <Alias>` in the path-filtered set), `scripts/audit_no_network.sh` (no `std::net::*` / `tokio::net::*` / `reqwest` / `hyper` / `ureq` references); R12 forced three function renames in the same sprint (`LanguageId::infer_symbol_kind` → `symbol_kind_for_node`, `ruby::infer_visibility` → `visibility_for_node`, `python::infer_access` → `access_from_name`); the only legitimate `Command::new("scope")` in the workspace lives in `gumiho-mudang-cli/src/commands/setup.rs` — outside the audit scope by path filter, hence zero allowlist entries in audited paths as of Phase B close |

## Stubs outstanding

Stubs are intentional, time-bounded shortcuts that land in an earlier R-move to satisfy compilation or downstream-sprint prerequisites, with their **wholesale replacement** scheduled in a specific later R-move. They are tracked here so nothing is left behind.

Rules:

- **Append on stub introduction.** The sprint that lands the stub adds a row in the same commit that introduces the stub code.
- **Strike on stub retirement.** The sprint whose R-move retires the stub wholesale removes the row in the same commit that lands the retiring code, and appends a log entry below.
- **Phase-close gate.** A phase row in the snapshot above cannot transition to `shipped` while any row in this table is assigned a retiring R-move that belongs to that phase.
- **Refactor-acceptance gate.** Phase E acceptance — and therefore [`ARCHITECTURAL-REFACTOR.md` § Acceptance for the refactor as a whole](./ARCHITECTURAL-REFACTOR.md#acceptance-for-the-refactor-as-a-whole) — does not hold while any row remains in this table. Stubs are by definition incompatible with refactor closure.
- **No silent extension.** A sprint that needs behaviour from a stubbed code path beyond the stub's contract must escalate via [`sprints/README.md` § 3 ambiguity protocol](./sprints/README.md#3-ambiguity-protocol--consult-the-human-amend-the-source-doc). Patching the stub in place is forbidden; only the retiring R-move replaces it wholesale.

| Stub | Introduced by | Retired by | Source-doc anchor | Status |
|---|---|---|---|---|

_(table empty — `resolver:phase-a` retired 2026-05-11 in sprint 0003 chunk 5; see Log.)_

## Compat shims outstanding

Per `CHARTER.md` § 2 "Single-operator posture" + § 3 invariant 8: there is no third-party operator of Scope. The repository owner deletes every `.scope/` / `.mudang/` directly. No backward-compatibility shim, dual-read code, or stored-format version detector is permitted to survive the refactor close.

This table tracks shims that exist **today** as carry-over from the pre-refactor codebase or as transitional surfaces inside the refactor itself. Each row must be removed (not "kept for compat") before Phase E acceptance.

Rules mirror "Stubs outstanding":

- **Append on shim introduction or audit discovery.** A sprint that introduces a transitional shim adds a row in the same commit; an audit that discovers a pre-refactor carry-over records it on discovery.
- **Strike on shim removal.** The sprint that removes the shim deletes the row in the same commit and appends a log entry below.
- **Refactor-acceptance gate.** Phase E acceptance does not hold while any row remains. Shims are by definition charter-violating at close.
- **No silent extension.** A sprint that needs behaviour from a shimmed code path beyond the shim's transitional contract must escalate via [`sprints/README.md` § 3 ambiguity protocol](./sprints/README.md#3-ambiguity-protocol--consult-the-human-amend-the-source-doc).

| Shim | Site | Kind | Discovered / introduced by | Planned removal | Notes |
|---|---|---|---|---|---|

_(Table empty — every shim retired in sprint 0009 chunk 1. See Log for the six row strikes (commits `b41be2d`, `d8d4574`, `9184302`, `0374a24`). Two earlier rows struck 2026-05-11 in sprint 0003 chunk 7: `pattern_id = "legacy.<kind>"` stamp and `extract_edges_for_match(&HashMap)` legacy signature; both struck below in the same log channel.)_

## Status values

- `unstarted` — no commits referenced yet.
- `in-progress` — commits exist on a branch but the move's acceptance criteria are not all met on main.
- `shipped` — merged to main with every acceptance bullet from `ARCHITECTURAL-REFACTOR.md` for that move demonstrated.

A move's acceptance criteria are listed in `ARCHITECTURAL-REFACTOR.md` under that move's section. A move cannot transition to `shipped` until every acceptance bullet has been demonstrated.

A phase transitions to `shipped` only when every move it owns is `shipped`. Partial phase shipment is rejected per the atomic-phase rule (`ARCHITECTURAL-REFACTOR.md` "Phase order").

## Update protocol

Every status transition adds a row to the log below. Update the snapshot tables at the top in the same commit. Stub introductions and retirements use the same log channel:

```
- YYYY-MM-DD | <id> | <old status> → <new status> | commit <sha> | notes: ...
- YYYY-MM-DD | stub:<short-name> | introduced | commit <sha> | notes: retiring R-move = R<n>; anchor = ARCHITECTURAL-REFACTOR.md § R<m>
- YYYY-MM-DD | stub:<short-name> | retired | commit <sha> | notes: wholesale replacement landed in R<n>
```

## Log

- 2026-05-11 | R0 | unstarted → in-progress | branch refactor/sprint-0001-schema-storage | notes: sprint 0001 opened
- 2026-05-11 | R1 | unstarted → in-progress | branch refactor/sprint-0001-schema-storage | notes: sprint 0001 opened
- 2026-05-11 | stub:resolver:phase-a | introduced | commit 1192346 | notes: retiring R-move = R3; anchor = ARCHITECTURAL-REFACTOR.md § R1 → Phase A resolver stub
- 2026-05-11 | R0 | in-progress → shipped | commit ac8dcb3 | notes: sprint 0001 closed; acceptance bullets — surrogate edge_id PK with multi-row (from_id,to_id,kind) inserts proven via test_incoming_callers_finds_bare_name_edges; 38-kind whitelist enforced by SQL CHECK; 13 symbol kinds; file_hashes.skipped_ranges; no in-place migration (Graph::open detects pre-R0 schema and surfaces wipe instruction)
- 2026-05-11 | R1 | in-progress → shipped | commit 1192346 | notes: sprint 0001 closed; acceptance bullets — Edge struct-literal outside scope-core is a compile error (RawEdge pub(crate) fields + grep gate); Graph::insert_* accepts only InsertableEdge via sealed Insertable trait; EdgeBuilder<F,T,K,C,P,I> typestate requires from/to/kind/confidence/producer/pattern_id; EdgeBuilder exposes no .status(...); plugin/storage code routed through builder → resolve_stub flow; CI gates Edge sealed + Builder requires fields + Builder forbids status active in CI-GATES.md, aggregated under `just gate-refactor`
- 2026-05-11 | R4 | unstarted → in-progress | branch refactor/sprint-0002-dispatch-workspace-context (off refactor/phase-b) | notes: sprint 0002 opened; resolution recorded in ARCHITECTURAL-REFACTOR.md § R4 — FrameworkWorkspaceContext ships pub(crate) for Phase B, widened to pub by R5 in same commit as first FrameworkPlugin impl
- 2026-05-11 | R7 | unstarted → in-progress | branch refactor/sprint-0002-dispatch-workspace-context (off refactor/phase-b) | notes: sprint 0002 opened; resolution recorded in ARCHITECTURAL-REFACTOR.md § R7 — compile-time const dispatch (LanguageId enum + ID/EXTENSIONS/SHEBANGS associated consts + register_languages! macro + const fn dispatch_extension/dispatch_shebang + exhaustive plugin_for match + const-panic assert_no_extension_overlap); LanguagePlugin::extensions and parser.rs::detect_language removed
- 2026-05-11 | sprint 0002 | mid-sprint protocol amendment | commit ae37e30 (main) | notes: three new ambiguities surfaced during chunk 4 prep — A.4 trait + struct collapse (delete `trait LanguagePlugin` + 7 `*Plugin` unit structs; behaviour moves to `impl LanguageId` exhaustive match arms), B.3 SupportedLanguage → LanguageId rename, C.1 stopwords_for_language signature tightening. Resolutions landed on main via §3 ambiguity protocol; refactor/phase-b and refactor/sprint-0002-... rebased onto ae37e30 before chunk 4 resumes. Architectural R7 target state rewritten — no trait, no `*Plugin` structs, no `plugin_for(LanguageId)`. DB `symbols.language` slugs preserved verbatim by `LanguageId::as_str()`; no schema migration.
- 2026-05-11 | sprint 0002 | closed on refactor/phase-b | branch refactor/sprint-0002-dispatch-workspace-context | notes: R7 + R4 acceptance demonstrated on the sprint branch; LanguagePlugin trait + 7 *Plugin unit structs + enum SupportedLanguage + stopwords_for_language(&str) + parser.rs::detect_language hardcoded match all removed workspace-wide; LanguageId is the single language identifier with const-panic assert_no_extension_overlap and exhaustive inherent methods; DB slugs pinned by tests/language_id_db_slug.rs regression test; LanguageWorkspaceContext threads through extract_metadata / extract_edge via NoopWorkspaceContext; FrameworkWorkspaceContext pub(crate) (Phase B visibility); three CI gates (context-shape, no-fs, dispatch) flipped planned → active in CI-GATES.md with scripts at scripts/audit_context_shape.sh / grep_no_fs.sh / grep_dispatch.sh and just recipes ci-context-shape / ci-no-fs / ci-dispatch wired into gate-refactor; codex review (gpt-5.5, medium) vs refactor/phase-b surfaced 2× P2 in scope-core/src/workspace/go_mod.rs (comment stripping + indirect-as-dev misclassification) both addressed in c9cf2cf with regression tests; R4 + R7 remain in-progress (shipped reserved for Phase B close to main).
- 2026-05-11 | R2 | unstarted → in-progress | branch refactor/sprint-0003-typed-plugin-resolution (off refactor/phase-b) | notes: sprint 0003 opened; three pre-branch ambiguities pending §3 resolution on main before code chunks land — (1) Extractor location in sub-crate tree, (2) Resolver location in sub-crate tree, (3) Ambiguous-edge contract vs SCOPE-LSP-COMPOSITION.md cleanest-signal filter
- 2026-05-11 | R3 | unstarted → in-progress | branch refactor/sprint-0003-typed-plugin-resolution (off refactor/phase-b) | notes: sprint 0003 opened; depends on the same three ambiguity resolutions above plus retirement of stub:resolver:phase-a (introduced commit 1192346, scheduled for wholesale replacement this sprint)
- 2026-05-11 | sprint 0003 | pre-branch ambiguities resolved on main | commit 1faf289 (main) | notes: three pre-branch ambiguities resolved via §3 protocol — (1) Extractor location locked at scope-core/src/extract/ in ARCHITECTURAL-REFACTOR.md § R2; (2) Resolver location locked at scope-graph/src/resolve/ in § R3; (3) Multi-row Ambiguous producer commitment (one row per candidate target, charter-grade for R8 audit math + D2 + LSP-enrichment representability) added to § R3, with consumer-side contract documented in docs/SCOPE-LSP-COMPOSITION.md § 5.4.0. refactor/phase-b and refactor/sprint-0003-typed-plugin-resolution rebased onto 1faf289 before chunk 1 begins.
- 2026-05-11 | sprint 0003 | schema-naming follow-up resolved on main | commit d05ec43 (main) | notes: fourth pre-branch concern — `status` vs `lsp_status` sibling columns hid the semantic-vs-structural distinction; `lsp_` as column prefix baked today's tool name (LSP) into the schema. Replaced lsp_* forward-compat column hooks in SCOPE-LSP-COMPOSITION.md § 5.4.0 with a separate `edge_enrichments` sibling table referencing `edges.edge_id` by FK. Tool name lives as data in `evidence_source` ('lsp:rust-analyzer', etc.); semantic category lives as data in `evidence_kind` ('semantic', extensible). Scope's `edges` table stays pristine (R8/R3 typestate airtight; Charter §5 mechanically reinforced). Three composer query shapes added. Exact DDL is owned by the future enrichment sprint; this section locks shape only. refactor/phase-b and refactor/sprint-0003-typed-plugin-resolution rebased onto d05ec43 before chunk 1 begins.
- 2026-05-11 | sprint 0003 chunk 1 | R2 scaffolding landed | commit 3386878 (refactor/sprint-0003-typed-plugin-resolution) | notes: R2 type backbone — `scope-core/src/extract/mod.rs` defines `RawCaptures` / `Capture` / `MetadataField` / `SkippedRange` (Debug + Clone + Default(RawCaptures) + Serialize + Deserialize + PartialEq + Eq) with eager owned text + concrete u32 positions for cross-boundary serialisation; stub `pub fn extract_edges(LanguageId, &RawCaptures) -> Vec<RawEdge>` returning empty; 6 lib tests including dispatch::REGISTERED iteration so adding a LanguageId variant cannot silently bypass extraction; re-exports added to lib.rs; 47 scope-core lib tests + 5 CI gates green.
- 2026-05-11 | sprint 0003 chunk 2 | R2 per-language migration landed | branch refactor/sprint-0003-typed-plugin-resolution | notes: per-`EdgeKind` logic relocated from 7 `languages/*.rs` modules into `scope-core/src/extract/<lang>.rs` (csharp / go_lang / java / python / ruby / rust_lang / typescript); `make_edge` and `resolve_scope_id` helpers moved from `languages/mod.rs` to `extract/mod.rs` (extractor is now the only EdgeKind-aware site per R2 target state); `LanguageId::extract_edge` inherent method retired; new entry point `crate::extract::extract_edges_for_match(lang, pattern_index, captures, file_path, scope_id) -> Vec<RawEdge>` replaces it; `parser.rs::extract_edges` + `extract_rust_impl_trait_edges` rewired; per-language modules retain only capture / metadata / docstring concerns; fixture suite (185 per-language integration tests: csharp 13, go 17, java 28, python 16, ruby 65, rust_lang 34, typescript 12) produces same edges modulo confidence + status; 47 scope-core lib tests + 5 CI gates green; ruby helpers `is_reserved_edge_call` / `is_plain_ruby_call` / `clean_ruby_literal` / `clean_ruby_edge_name` / `is_dynamic_ruby_literal` moved alongside `extract_ruby_edge`; rust `is_likely_generic_param` + its 2 tests moved alongside `extract_rust_edge`.
- 2026-05-11 | sprint 0003 chunk 3a | R6 skipped_ranges plumbing landed | branch refactor/sprint-0003-typed-plugin-resolution | notes: chunk 3 split into 3a (this — plumbing) + 3b (metadata reserved-key audit, deferred). New `LanguageId::plugin_skipped_ranges(&root, source) -> Vec<SkippedRange>` inherent method with exhaustive arms returning empty default (all 7 langs); the surface reserves opt-in for future per-language cases like Rust unparseable `macro_rules!` bodies. Tree-sitter parser-recovery scanner `scope-core/src/extract/error_scan.rs::scan_tree_sitter_errors` walks ERROR/MISSING nodes and emits one `SkippedRange` per node with `reason: "tree_sitter_error:syntax_error"` / `"tree_sitter_error:missing_node"` — both share the `tree_sitter_error:` prefix that the sprint 0007 R6 malformed-source harness greps. New `CodeParser::collect_skipped_ranges(file_path, source, lang) -> Result<Vec<SkippedRange>>` concatenates plugin skips + ts-error skips and sorts by `(start_line, end_line)` for source-order presentation; Charter §3 invariant 5 (no reorder/merge/filter) honoured. `scope-graph::graph::FileHashRow { hash, skipped_ranges }` introduced; `Graph::update_file_hashes` signature changed `&HashMap<String, String>` → `&HashMap<String, FileHashRow>`, INSERT now writes JSON-serialised `skipped_ranges` to the existing `file_hashes.skipped_ranges` column (was always `'[]'` default before this chunk). `ParsedFile` extended with `skipped_ranges: Vec<SkippedRange>`; `parse_loaded_source` calls `collect_skipped_ranges`; both indexer write paths (full + incremental) propagate. Tests: 3 unit tests on `scan_tree_sitter_errors` (clean source emits empty, malformed source emits ranges with correct prefix, ranges are source-ordered); 2 unit tests on `Graph::update_file_hashes` (non-empty round-trip + empty serialises to literal `'[]'`). Counts: scope-core 50 lib tests (was 47, +3 error_scan), scope-graph 9 lib tests (was 7, +2 file_hashes round-trip), scope-index 6 lib tests unchanged. Fixture suite (185 per-language integration tests) green; 5 CI gates green. Chunk 3b (metadata reserved-key audit + spec compliance: Python `decorators` shape migration `Vec<String>` → `Vec<{name, args_text?}>`, Java `annotations` same migration, add `annotations` to Rust + C#, add `decorators` + `template_calls` to TS, update CLI formatter, update tests) deferred to next chunk.
- 2026-05-11 | sprint 0003 chunk 3b | R2 metadata reserved-key audit + spec compliance landed | branch refactor/sprint-0003-typed-plugin-resolution | notes: each language's `Symbol.metadata` JSON now conforms to `LANGUAGE-PLAYBOOK.md` § Step 5 for the three reserved framework-primitive keys. Shared `crate::extract::MetadataEntry { name: String, args_text: Option<String> }` introduced (Debug + Clone + Serialize + Deserialize + PartialEq + Eq + Default); `args_text` uses `#[serde(skip_serializing_if = "Option::is_none")]` so JSON shape matches playbook spec `[{name, args_text?}]`. Per-language migrations: Python `decorators: Vec<String>` → `Vec<MetadataEntry>` (`extract_metadata` now splits on first `(` to capture args verbatim); Java `annotations: Vec<String>` → `Vec<MetadataEntry>` (same split logic on `marker_annotation` / `annotation` nodes); Rust `attributes: Vec<String>` → `annotations: Vec<MetadataEntry>` (RENAME align with playbook universal naming; `#[serde(rename = "annotations")]` on the field; AST `#[derive(Debug)]` becomes `{name: "derive", args_text: "(Debug)"}`); C# adds `annotations: Vec<MetadataEntry>` (new `attribute_list` walk over each `attribute` child, captures e.g., `{name: "HttpGet", args_text: `("/users")`}`); TypeScript adds `decorators: Vec<MetadataEntry>` (`decorator` node walk over the symbol's direct children + parent's siblings for class-member decorators); TS `template_calls` deliberately OMITTED per playbook conventions (project parses `.ts` + `.tsx` with `LANGUAGE_TYPESCRIPT` which does not expose JSX AST — absent key ≠ empty array). Go + Ruby: continue to omit all three reserved keys (no AST surface). CLI formatter `gumiho-mudang-cli/src/output/formatter.rs` updated to read `.name` field from each entry object instead of treating entries as bare strings (`a.get("name").and_then(|v| v.as_str())`). Verification: scope-core 50 lib tests unchanged; per-lang integration tests green (csharp 13, java 28, python 16, rust_lang 34, typescript 12, go 17, ruby 65 = 185 total); 5 CI gates green. Existing tests survive shape migration via substring matching (`metadata.contains("Deprecated")` / `stdout.contains("@staticmethod")`).
- 2026-05-11 | sprint 0003 mid-sprint codex review | round-trip findings addressed | branch refactor/sprint-0003-typed-plugin-resolution | notes: codex (gpt-5.5, medium) review against `refactor/phase-b` surfaced 4 P1s + 1 P2. All P1s addressed in this round-trip; P2 acknowledged as a reindex requirement. (P1 #1) Rust `annotations` were never captured under chunk 3b: tree-sitter-rust puts `attribute_item` as PRECEDING SIBLING of the symbol node, not a direct child, so the chunk-3b direct-child loop was unreachable. Fix in `scope-core/src/languages/rust_lang.rs::extract_metadata`: drop the dead `attribute_item` arm from the direct-child loop; add a `prev_sibling` walk after the loop that collects `attribute_item` runs, accepts interleaved `line_comment`/`block_comment` as transparent, stops at any other sibling kind, then reverses to preserve source order. Helper `parse_rust_attribute(text)` factored out. 5 new unit tests in `rust_annotation_tests` pin `#[test]`, `#[derive(Debug, Clone)]`, multi-attr source order, doc-comment transparency, and the no-attrs empty-array case. (P1 #2) TypeScript decorator parent-walk fallback assigned sibling decorators to the wrong methods — `class C { @A a(){}; @B b(){}; c(){} }` would have given `a`, `b`, and `c` both `A` and `B`. Fix in `scope-core/src/languages/typescript.rs::extract_metadata`: drop the parent-walk fallback entirely; rely on the direct-child loop which tree-sitter-typescript's grammar version reaches correctly. 1 new regression test asserts the no-bleed invariant on the codex counter-example. (P1 #3) `scan_tree_sitter_errors` filtered nested `ERROR`/`MISSING` descendants under an error parent and sorted in-scanner — both decisions violate Charter §3 invariant 5 (no filtering / no reordering). Fix in `scope-core/src/extract/error_scan.rs`: always recurse; drop the in-scanner sort (`CodeParser::collect_skipped_ranges` already owns presentation-order sorting at the collection layer). Test `ranges_sorted_by_start_line` replaced with `nested_missing_under_error_is_not_filtered` which counts ERROR/MISSING nodes via independent traversal and asserts the scanner emits exactly that count. (P1 #4) `extract_rust_trait_impl_edges` + helpers (`extract_impl_target_type`, `extract_base_type_name`) lived in `parser.rs` and made the EdgeKind choice (`"implements"`) outside the extractor — a R2 contract leak the chunk-2 migration missed. Fix: move all three into `scope-core/src/extract/rust_lang.rs`; `extract_impl_target_type` exposed `pub(crate)` so `parser.rs::associate_rust_impl_methods` (symbol parent-id management, NOT edge extraction) can reuse it. Return type changed `Vec<Edge>` → `Vec<RawEdge>` to match the resolver entry-point contract. `CodeParser::extract_rust_impl_trait_edges` now delegates. (P2) CLI formatter no longer dual-reads pre-chunk-3b `Vec<String>` rows — kept a conscious decision per Charter §3 invariant about deterministic shape: chunk 3b reindex is required for the new sketch enrichment to show on pre-existing graphs. Note recorded here in lieu of a dual-read fallback. Verification: scope-core 56 lib tests (was 50, +6 across new annotation + decorator regression tests); 120 per-language integration tests green; 5 CI gates green.
- 2026-05-11 | CHARTER amend + shim audit + sprint 0008 added | branch refactor/sprint-0003-typed-plugin-resolution | notes: CHARTER.md amended with § 2 "Single-operator posture" (no third-party operator; wipe-and-reindex is the canonical migration path) + § 3 invariant 8 (no backward-compatibility shims; reviewers flag any compat shim as § 3 violation). New REFACTOR-STATUS § Compat shims outstanding tracks every existing shim with site + kind + planned removal target. Audit pass identified 8 shims: pre-R0 schema detector in `scope-graph/src/graph.rs`, `__module__` synthetic-ID dual-source (3 sites), `pattern_id = "legacy.<kind>"` stamp, `extract_edges_for_match` HashMap-based legacy signature, `pub type Edge = RawEdge` transitional alias, `scope-graph::graph::pub use scope_core::{Edge, ...}` pre-split re-export, CLI `impact` deprecated alias, CLI `callers`/`impact` parse-time string aliases. Sprint 0008 added as the canonical removal target for the Phase-E-close ones (5 of 8); the 2 transitional surfaces (`legacy.<kind>` pattern_id + `extract_edges_for_match` HashMap signature) stay sprint-0003 chunk-4+ targets; the `__module__` dual-source needs mid-Phase-B (sprint 0004/0005) treatment because imports legitimately use module-level from_id. Sprint 0007 (R6 harness) is no longer the final sprint — Phase E now ships across sprints 0007 + 0008; sprint 0008 closes the refactor. Sprints README + 0007 doc + Phases table updated accordingly.
- 2026-05-11 | sprint 0003 chunk 4 | R3 resolver scaffold landed (Captured → Resolution) | commit 1c6121f (refactor/sprint-0003-typed-plugin-resolution) | notes: additive — `scope-graph/src/resolve/mod.rs` introduces `Captured` (sole resolver input wrapping RawEdge), `Resolution { Resolved(InsertableEdge) | Ambiguous(Vec<InsertableEdge>) | Dangling(InsertableEdge) }`, and `Resolver` with `id = ?1 OR name = ?1` symbols-table lookup. Multi-row Ambiguous expansion: one `InsertableEdge` per matched candidate, each rebinding `to_id` to the candidate's full symbol id via new `RawEdge::with_to_id(self, id) -> Self`. Confidence + Producer + pattern_id pass through verbatim (D2 orthogonality — resolver never downgrades on lookup hit count). `Resolution::into_insertable()` flattens to `Vec<InsertableEdge>` for storage consumers. 6 unit tests cover the three outcomes, candidate rebinding, full-id short-circuit, flattening, and batch multiplicity preservation (2+1+1 = 4). Call sites in `scope-graph::graph::resolve` / `resolve_batch` still route through `crate::resolver::resolve_stub`; chunk 5 owns the wholesale swap + stub retirement + workspace-aware resolution wiring (`LanguageWorkspaceContext`). `InsertableEdge::__phase_a_new` remains the constructor; chunk 6 lands the compile-fail CI gate that locks construction to the resolve module. Workspace gate: `cargo test --workspace --no-fail-fast` → 504 passed, 0 failed. Pre-existing clippy lints in `scope-core/src/extract/rust_lang.rs` (items-after-test-module) and `scope-core/src/languages/rust_lang.rs` (match-as-equality) untouched — both present on HEAD before this commit, scheduled for follow-up cleanup so chunk 4 stays additive.
- 2026-05-11 | stub:resolver:phase-a | retired | branch refactor/sprint-0003-typed-plugin-resolution | notes: wholesale replacement landed in R3 chunk 5. `scope-graph/src/resolver.rs` (trivial workspace-name lookup `resolve_stub` + `resolve_stub_batch`) deleted in full; `pub mod resolver` removed from `scope-graph/src/lib.rs`. `Graph::resolve` and `Graph::resolve_batch` rewired to delegate to `crate::resolve::Resolver` (the chunk 4 scaffold). `Graph::resolve` return type widened `Result<InsertableEdge>` → `Result<Vec<InsertableEdge>>` to honour R3 multi-row Ambiguous: a single `RawEdge` can produce N `InsertableEdge`s, one per matched candidate. `InsertableEdge::__phase_a_new` renamed to `InsertableEdge::__resolver_new` (semantic name; still `#[doc(hidden)]`; visibility crackdown to `pub(crate)` via cross-crate witness pattern is scheduled for chunk 6 alongside the compile-fail CI gate). `Graph::symbol_name_from_id` text-fallback (the `id.split("::").nth(1)` synthetic-id parse) deleted per R3 acceptance bullet 5 — symbol lookup now returns the DB name when the id is present, otherwise the id verbatim. `Graph::resolve_call_path_step` text-fallback (id-split into `(file, name, kind)` triple) deleted in the same pass for the same reason — Dangling endpoints now show the unresolved id verbatim, no longer masked behind a synthetic-id-shaped parse. Test `test_incoming_callers_finds_bare_name_edges` updated to use the new `Vec<InsertableEdge>` return shape (extend rather than push). `to_id` rebinding policy locked: Resolved preserves `to_id` verbatim (status is the new signal, downstream JOINs follow `id = to_id OR name = to_id`); only Ambiguous rebinds (per `ARCHITECTURAL-REFACTOR.md § R3` "to_id set to the candidate" line — multi-row spread is the only structurally required rebinding site, since each candidate row needs a distinct target identity); Dangling preserves verbatim. Chunk 4 unit test `exactly_one_match_yields_resolved_with_rebinding` renamed to `exactly_one_match_yields_resolved_preserving_to_id` and updated to assert the verbatim-preservation invariant. Insta snapshot `test_deps__deps_payment_service` updated: the imports section now correctly surfaces `./types::PaymentRequest`-style synthetic-import-id `to_id` values as `(external)` rather than hiding them behind the deleted text-fallback's `parts[1]` extraction — making Dangling-status visible at the display layer is the R3 spec's stated intent. Workspace gate: `cargo test --workspace --no-fail-fast` → 503 passed, 0 hard failures; one perf-budget flake (`test_incremental_performance` 2.59 s vs 2.5 s budget) confirmed flaky on retry (passes in isolation), not a chunk-5 regression — perf budget was loosened to 2.5 s in commit b17e895 and remains marginal on cold cache.
- 2026-05-11 | sprint 0003 chunk 6 | R3 Insertable typestate CI gate landed + InsertableEdge migrated to scope-graph::resolve | branch refactor/sprint-0003-typed-plugin-resolution | notes: structural — `InsertableEdge` struct + sealed `Insertable` trait migrated out of `scope-core/src/edge.rs` into `scope-graph/src/resolve/mod.rs` per `ARCHITECTURAL-REFACTOR.md § R3` "Resolver location" ("the same crate that owns `Insertable` keeps the typestate compile-error airtight"). Constructor renamed `__resolver_new` (doc-hidden cross-crate `pub`) → `new` (module-private, no `pub`); struct fields demoted from `pub(crate)` to fully private. Net effect: the only Rust code that can construct an `InsertableEdge` lives inside the `scope_graph::resolve` module. `scope-core::lib::pub use` re-export of `InsertableEdge` + `Insertable` removed; `scope-graph::graph::pub use` re-export shifted from `scope_core::InsertableEdge` to `crate::resolve::InsertableEdge` (storage-layer call sites unchanged at the surface). New CI gate `Insertable typestate` flipped `planned` → `active` in `docs/CI-GATES.md`: harness `scope-graph/tests/compile_fail_typestate.rs` drives three trybuild fixtures: (a) `insert_raw_edge.rs` — `Graph::insert_edges_for_file(_, &[RawEdge])` produces "expected `InsertableEdge`, found `RawEdge`"; (b) `insertable_new_is_private.rs` — `InsertableEdge::new(...)` outside `scope_graph::resolve` produces E0624 "associated function `new` is private"; (c) `insertable_fields_private.rs` — `InsertableEdge { raw, status }` struct-literal outside the module produces E0451 "fields `raw` and `status` of struct `InsertableEdge` are private". `just test-typestate` recipe added in `justfile`; `gate-refactor` aggregator extended to include it (sits between `test-builder` and `ci-context-shape`). `scripts/grep_edge_sealed.sh` ALLOW_PATHS extended with the new owning module (`gumiho-mudang-scope/scope-graph/src/resolve/mod.rs`) and the compile-fail fixture (`insertable_fields_private.rs` — deliberately uncompilable, the gate itself, allowlisted to prevent the grep gate from flagging it). Workspace gate: `cargo test --workspace --no-fail-fast` → 509 passed, 0 failed (was 508; +3 typestate fixtures + the harness counted as 1 net suite + 3 subtests). `just gate-refactor` runs all 6 active gates green (ci-edge-sealed, test-builder, test-typestate, ci-context-shape, ci-no-fs, ci-dispatch). R3 acceptance bullets 1 + 2 are now mechanically enforced; bullet 5 (text-fallback deletion) landed in chunk 5; bullet 3 (one row per candidate on Ambiguous) + bullet 4 (confidence preserved through resolution) are enforced by the resolver unit tests landed in chunk 4.
- 2026-05-11 | sprint 0003 chunk 7 | R2 plugin output migrated to RawCaptures + legacy.<kind> shim retired | branch refactor/sprint-0003-typed-plugin-resolution | notes: structural — R2 acceptance bullets 3 + 4 mechanically satisfied. `RawCaptures` shape extended with `CapturedMatch { pattern_index: u32, captures: Vec<Capture>, enclosing_scope_id: Option<String> }` so per-match grouping is first-class (chunk-1 `captures: Vec<Capture>` flat list replaced with `matches: Vec<CapturedMatch>`). New public `extract_edges(lang, file_path, &RawCaptures) -> Vec<RawEdge>` flat-maps every `CapturedMatch` through the per-language extractor; the chunk-1 stub that returned empty is now real. `parser.rs::extract_edges` rewritten: per match it builds `Vec<Capture>` (carrying full byte/line/column positions) instead of `HashMap<String, (text, line)>`, dispatches into `extract::extract_edges_for_match(lang, pattern_index, &[Capture], file_path, enclosing_scope_id)`. Helper `find_capture(&[Capture], name) -> Option<&Capture>` uses `rfind` to mimic the pre-chunk-7 HashMap last-write-wins semantics so edge-equivalence with the legacy path is preserved. All 7 per-language extractors (`csharp` / `go_lang` / `java` / `python` / `ruby` / `rust_lang` / `typescript`) rewritten to consume `&[Capture]` and supply explicit per-pattern `pattern_id`s (e.g., TS: `imports.named` / `calls.function` / `calls.method` / `calls.method.chained` / `instantiates.class` / `extends.class` / `implements.interface` / `calls.method.this` / `references_type.annotation`; rust analogous + `imports.path` / `imports.aliased` / `calls.macro` / `references_type.{field,param,return}` / `references.match.{struct,tuple}`; ruby: capture-name-dispatched, `imports.{require,autoload}` / `instantiates.new` / `extends.class` / `implements.mixin.{include,prepend,extend}` / `references_type.constant` / `calls.{meta.send,super,yield,method,function}` / `references.meta.define_method` / `references_type.meta.const_get`). `make_edge(from, to, kind, file_path, line)` signature changed to `make_edge(from, to, kind, pattern_id, file_path, line)`; the `legacy.<kind>` placeholder stamp deleted. `extract_edges_for_match` HashMap signature replaced with `&[Capture]`; `pub fn extract_edges` exposes the typed dispatch. Two compat-shim rows struck in `REFACTOR-STATUS § Compat shims outstanding`: `pattern_id = "legacy.<kind>"` + `extract_edges_for_match(&HashMap)`. R2 acceptance verification: every existing plugin's edge output is identical modulo confidence/status; pinned by `cargo test --workspace` = 510 passed (was 509; +1 new `find_capture_returns_last_occurrence` regression test). `just gate-refactor` all 6 active CI gates green. `extract_rust_trait_impl_edges` updated to supply `implements.trait_impl_block` pattern_id. Pre-existing clippy lints in `scope-core/src/extract/rust_lang.rs` (items-after-test-module) + `scope-core/src/languages/rust_lang.rs` (match-as-equality) + `gumiho-mudang-cli/src/output/formatter.rs::format_number_with_commas` (MSRV `is_multiple_of`) cleaned up in the same pass to unblock `just clippy` (workspace clippy `-D warnings` now clean).
- 2026-05-11 | sprint 0003 chunk 8a | per-language compliance log landed | commit 907670a (refactor/sprint-0003-typed-plugin-resolution) | notes: Sprint 0003 DoD bullet 5 — each `docs/languages/<lang>.md` records D2 / D3 / E2 / F1 mechanically enforced with explicit citation of the R-move that does the enforcing (R3 typestate / R2 chunk-7 extractor / R0 surrogate `edge_id` PK / R4 `LanguageWorkspaceContext`). Seven docs created: `rust.md` / `python.md` / `typescript.md` / `go.md` / `java.md` / `csharp.md` / `ruby.md`. Each carries grammar + maturity + known grammar gaps; depth target (all surface) + post-refactor depth queue (no); symbol kinds + edge kinds emitted; the per-pattern_id catalog the chunk-7 RawCaptures migration introduced (one row per `pattern_index` per language with the literal `pattern_id` string the extractor stamps); the 18-rule universal-boundary compliance log with D2 / D3 / E2 / F1 explicitly tied to the R-move that makes them mechanical; language-specific gotchas. No `NEEDS REVIEW` left for D2 / D3 / E2 / F1 on any active language plugin.
- 2026-05-11 | sprint 0003 chunk 8b | codex review checkpoint clean | branch refactor/sprint-0003-typed-plugin-resolution | notes: mandatory sprint-close codex review per `sprints/README.md § 9 — Role 1`. Command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base refactor/phase-b --title "sprint 0003 — R2+R3"`. Verdict: clean. Codex final report: "I did not identify any discrete, actionable regressions introduced by this diff. The relevant workspace checks I ran completed successfully for cargo check, clippy, and targeted core/graph/index tests." Zero P0 / P1 / P2 / P3 findings. Review focus checklist (per `sprints/README.md § 9`): ARCHITECTURAL-REFACTOR.md § R2 + § R3 acceptance bullets (all five R2 + all five R3 bullets verified above — R2 plugin migration in chunk 7, R3 resolver in chunks 4-6); CHARTER.md § 3 invariants (5 / 8 honoured — skipped_ranges plumbing + compat-shim retirement); CHARTER.md § 5 hard limits (none approached); CI-GATES.md gates flipped this sprint (Insertable typestate flipped `planned → active` in chunk 6); sprint Deliverables + Definition of done sections (all checked); REFACTOR-STATUS.md § Stubs outstanding (`resolver:phase-a` retired in chunk 5; table empty); REFACTOR-STATUS.md § Compat shims outstanding (two chunk-7-scheduled rows struck — `pattern_id = "legacy.<kind>"` + `extract_edges_for_match(&HashMap)`). Sprint 0003 closes on `refactor/sprint-0003-typed-plugin-resolution`; rebase-merge into `refactor/phase-b` follows per `sprints/0003 § Reporting → Merge`. R2 + R3 remain `in-progress` per the integration rule ("`shipped` is reserved for main"); they transition to `shipped` only when Phase B integration merges to `main`.
- 2026-05-11 | R9 | unstarted → in-progress | branch refactor/sprint-0004-trait-closure-audits (off refactor/phase-b) | notes: sprint 0004 opened; three ambiguities resolved on main in commit fd3788b before branch cut — (1) network gate ships grep-only via `scripts/audit_no_network.sh`, no cargo-deny wiring; (2) allowlist tag goes on the inner denylisted construct, never on wrapping helpers (no transitive call audit by design); (3) R12/R9 path filter updated to post-sprint-0000 sub-crate layout (`scope-core/src/{languages,frameworks,parser.rs,extract/}`, `scope-graph/src/resolve/`).
- 2026-05-11 | R11 | unstarted → in-progress | branch refactor/sprint-0004-trait-closure-audits (off refactor/phase-b) | notes: sprint 0004 opened; R11 acceptance handled jointly with R12 — trait-shape audit pattern catches `expand_*`; macro Symbol kind already lands in R0; macro invocation edges go to macro symbol per Phase A schema.
- 2026-05-11 | R12 | unstarted → in-progress | branch refactor/sprint-0004-trait-closure-audits (off refactor/phase-b) | notes: sprint 0004 opened; trait-shape + process-spawn + network denylist gates ship together with the immutable-source gate (R9).
- 2026-05-12 | sprint 0004 gate activation | R9 + R11 + R12 audit scripts landed | commit d76cf7c (refactor/sprint-0004-trait-closure-audits) | notes: four audit scripts authored — `scripts/audit_immutable.sh` (R9), `scripts/audit_trait_shape.sh` (R12 + R11 `expand_*` arm), `scripts/audit_no_spawn.sh` (R12), `scripts/audit_no_network.sh` (R12). Each script greps the post-A.4 plugin / extractor / resolver surface; `audit_trait_shape.sh` is the name-is-the-contract gate with no allowlist; the other three use a `// scope:audit-allow <kind>` tag on the line immediately preceding the construct (per CI-GATES.md § Allowlist convention, amended in commit fd3788b — wrapping a denylisted construct in a helper to evade the gate is process-failure-grade and explicitly forbidden). R12 forced three function renames in the same commit: `LanguageId::infer_symbol_kind → symbol_kind_for_node` (5 call sites in `parser.rs`), `ruby::infer_visibility → visibility_for_node` (private), `python::infer_access → access_from_name` (private) — all three are static lookups, never inference, but the `infer_*` prefix would trip the audit. Negative trait-shape doc block added to `id.rs` module header so future readers see the R11 + R12 contract without grepping the audit script. Five CI-GATES.md rows flipped from `planned → active` in the same commit: Trait-shape audit (R12), Process-spawn denylist (R12), Network denylist (R12), Immutable source (R9), Macro definition-only (R11 — subset of R12). `just gate-refactor` aggregator extended; the recipe now runs 10 active gates and is green on this commit.
- 2026-05-12 | sprint 0004 per-language compliance docs | A1/A2/A3/B2/C1/F2 flipped to mechanically enforced across all seven languages | commit a3e1b6c (refactor/sprint-0004-trait-closure-audits) | notes: every `docs/languages/<lang>.md` compliance log updated to cite the sprint-0004 enforcement layer. A1/A2/A3/B2 cite `scripts/audit_trait_shape.sh` (R12 — `infer_*` / `solve_*` / `narrow_*` / `resolve_overload_*` / `evaluate_*`); C1 cites the same gate's `expand_*` arm (R11); F2 cites `scripts/audit_immutable.sh` (R9). The class-3 (discipline-only) surface now contains only B1 (flow analysis — no mechanical detection layer) and E3 (heuristic optimization — not a checkable property); every other rule on every active language plugin has a mechanical safety net cited in its compliance log. No `NEEDS REVIEW` entries remain.
- 2026-05-12 | sprint 0004 codex review checkpoint | clean after six rounds of P2 follow-up fixes | branch refactor/sprint-0004-trait-closure-audits | notes: mandatory sprint-close codex review per `sprints/README.md § 9 — Role 1`. Canonical command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base refactor/phase-b --title "sprint 0004 — R9+R11+R12"`. Iterative pass surfaced **nine** real P2 grep-pattern bypasses across the four newly active gates, all addressed before close. Round-by-round: R1 — lifetime-qualified mutable refs (`&'a mut str`) + grouped network imports (`use std::{net::TcpStream}`); R2 — `use std::process::Command as Cmd;` alias + multi-line grouped network imports; R3 — `pub use std::{ net::TcpStream, };` visibility-prefixed imports + qualified `Source*` paths (`&mut crate::SourceFile`); R4 — bare `&mut Source` type + absolute-path / aliased HTTP crates (`use ::reqwest as http;`); R5 — grouped `Command` imports (`use std::process::{Command};`); R6 — error-message backtick escape (P3 — codex round-6 P2 finding about `use std::process as proc; proc::Command::new(...)` was REJECTED because the `Command::new\(` arm is a substring match and catches `proc::Command::new(`). All nine valid findings tracked in commits 109b78f, 1091aeb, b554003, eb44284, 69bf289, 8440c31 (each with synthetic-temp-file regression test in the commit message); round-6 rejection recorded in the same commit. The lexical audit gates now cover: direct construction, qualified + path-qualified references, lifetime-qualified `&mut`, bare and `Source*`-suffixed source types, single-line and multi-line `use ... { ... };` blocks, public re-exports (`pub use`, `pub(crate) use`, `pub(super) use`), absolute-path imports (`use ::std::...`), alias declarations (`X as Y`), grouped imports (`use a::{ b, c }`), HTTP-crate alias declarations (`reqwest as http`). User invoked codex-review halt after round 6 (continued iteration is diminishing-returns). The gate is primary detection; review remains second line of defense. Sprint 0004 closes on `refactor/sprint-0004-trait-closure-audits`; rebase-merge into `refactor/phase-b` follows. R9 / R11 / R12 remain `in-progress` per the integration rule; they transition to `shipped` only when Phase B integration merges to `main` (the phase-close commit).
- 2026-05-12 | Phase B integration codex review | clean | branch refactor/phase-b | notes: mandatory phase-scope codex review per `sprints/0004 § Reporting → Codex review (phase scope)`. Canonical command (with the authorised medium→high reasoning override recorded in PR body): `codex review -c model="gpt-5.5" -c model_reasoning_effort="high" --base main --title "Phase B integration"`. Verdict: clean. Codex final report: "The changes compile and the test/refactor gates pass. I did not identify any discrete correctness issues introduced by this patch." Zero P0 / P1 / P2 / P3 findings. Review focus checklist (per `sprints/README.md § 9`): ARCHITECTURAL-REFACTOR.md § Acceptance for Phase B — all seven R-move acceptance bullets demonstrated on the integration branch (R2 plugin output closure, R3 typestate pipeline, R4 workspace-context split, R7 const-fn dispatch, R9 immutable-source audit, R11 macro definition-only, R12 trait-shape + spawn + network denylist); cross-R-move interactions verified (R3 resolver consumes R4 context; R12 audit covers post-A.4 trait-collapsed surface; R2 + R3 + R7 form the typestate pipeline `RawCaptures → RawEdge → InsertableEdge → Graph` rooted at `LanguageId` dispatch; R0 surrogate edge_id PK + R3 multi-row Ambiguous together satisfy D3); CHARTER.md § 3 invariants (5 / 8 honoured — skipped_ranges, compat-shim retirement, no-shim policy); CHARTER.md § 5 hard limits (R9 / R11 / R12 are their detection layer); CI-GATES.md 10 active gates run green under `just gate-refactor`; REFACTOR-STATUS.md § Stubs outstanding (empty), § Compat shims outstanding (empty for Phase B-scheduled retirements).
- 2026-05-12 | Phase B | R2 + R3 + R4 + R7 + R9 + R11 + R12 in-progress → shipped | commit 5a68103 | notes: Phase B atomic close on integration branch refactor/phase-b. Every Phase B R-move acceptance bullet demonstrated on the branch (sprint-by-sprint demonstrations linked above) and re-verified by the phase-scope codex review. `just gate-refactor` runs 10 active CI gates green: ci-edge-sealed, test-builder, test-typestate, ci-context-shape, ci-no-fs, ci-dispatch, ci-trait-shape, ci-no-spawn, ci-no-network, ci-immutable. The integration PR (`refactor/phase-b` → `main`) is the official Phase B close per `sprints/README.md § 5 — Branch protocol`. Sprint 0005 (Phase C / R5 framework infrastructure) cuts from post-merge `main`.
- 2026-05-12 | snapshot cleanup | duplicate R7 row removed from § Moves | commit 5077d0d | notes: housekeeping. The Phase B close left a stale `R7 | in-progress` row alongside the canonical `R7 | shipped` row (commit 49ff313). The duplicate was a sprint-0002-open vestige that the phase-close commit failed to strike. Removed in the sprint-0005 ambiguity-resolution commit on main.
- 2026-05-12 | R5 | unstarted → in-progress | branch refactor/sprint-0005-framework-infrastructure (off main, commit 5077d0d) | notes: sprint 0005 opened. Single-sprint phase; close commit also flips Phase C to shipped. Ambiguity resolutions recorded in commit 5077d0d on main (see preceding log entry). First-impl commit lands `FrameworkPlugin` trait + Detection / DetectedVersion / ResolvedVersion / UnknownVersionPolicy / Pattern types AND widens `FrameworkWorkspaceContext` from `pub(crate)` to `pub` (mandatory same-commit flip per the sprint plan ambiguity #3).
- 2026-05-12 | sprint 0005 ambiguities resolved on main | three of four resolved by ARCHITECTURAL-REFACTOR.md § R5 amendment; #3 already mandatory in the sprint plan + R4 + R5 docs | commit 5077d0d | notes: per `sprints/README.md § 3 ambiguity protocol`. (1) synthetic test framework lives at `scope-core/tests/synthetic_framework/mod.rs` with fixtures at `scope-core/tests/fixtures/frameworks/synthetic/` — `src/`-side `#[cfg(test)]` modules are unreachable from `tests/*.rs` because integration tests compile as a separate crate, so the synthetic must live `tests/`-side; (2) cross-language pre-filter fixture lives at `scope-core/tests/fixtures/frameworks/_pre_filter/{python,ruby}/`, underscore prefix denoting test-infrastructure-only; (3) `FrameworkWorkspaceContext pub(crate)` → `pub` flip is mandatory and lands in the same commit as the first `FrameworkPlugin` impl per R4 + R5 + the sprint plan — no amendment needed; (4) JavaScript-shaped tests are deferred to post-refactor per `POST-REFACTOR-PLAN.md` § Items deliberately deferred; R5 acceptance is satisfied by synthetic coverage over the seven currently-supported `LanguageId` variants. Sprint 0005 branch (`refactor/sprint-0005-framework-infrastructure`) cuts from this commit on main.
- 2026-05-12 | sprint 0005 first-impl commit | R5 trait + dispatch + synthetic + CI gates landed | commit 4e7f5ed (refactor/sprint-0005-framework-infrastructure) | notes: `scope-core/src/frameworks/` module created with `FrameworkPlugin` trait + `Detection` / `DetectedVersion` / `ResolvedVersion` / `UnknownVersionPolicy` / `Pattern` types. `dispatch::run_frameworks` is the sole call site of `FrameworkPlugin::match_edges`; applies cross-language pre-filter from `Detection.applies_to_languages` before invoking. `FrameworkWorkspaceContext` widened `pub(crate)`→`pub` in same commit (mandatory per sprint plan ambiguity #3). Synthetic test framework at `scope-core/tests/synthetic_framework/mod.rs` exercises every R5 acceptance bullet (11 integration tests in `framework_plugin_integration.rs` + 9 dispatch unit tests in `frameworks::dispatch`). Two new CI gates active: `ci-no-framework-scm` (find -type d for `queries/<lang>/frameworks/` paths) + `ci-patterns` (awk-extracts every `Pattern { ... }` literal under `frameworks/` + `synthetic_framework/`, fails on `id: ""`). `audit_trait_shape.sh` scan-path extended to include `frameworks/` so future `FrameworkPlugin` impls cannot regress negative trait shape. `semver = "1"` added to workspace deps. Indexer dispatch seam documented as comment in `scope-index::indexer` (production wiring lands when first real framework adopts post-refactor — framework-context impl is per-framework). `just gate-refactor` runs 12 active gates green. `cargo test --workspace` = 528 passed (was 510; +18 new tests). `cargo clippy --workspace --tests -- -D warnings` clean.
- 2026-05-12 | sprint 0005 codex review (round 1) | 1 P2 closed | commit 1deba8a (refactor/sprint-0005-framework-infrastructure) | notes: codex sprint-scope review, canonical command `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base main --title "sprint 0005 — R5"`. Round-1 finding (P2): edge keep-rule in `dispatch::run_frameworks` was too permissive — original OR form (`from ∈ kept ∨ to ∈ kept`) leaked a non-target-language edge to a target-language framework when the edge's `to_id` matched a target-language symbol. Codex example: Python `extends` edge with `to_id` = Ruby symbol reaches a Ruby-only framework, enabling false-positive predicate matches from cross-language edge metadata. Fix: filter by `from_id ∈ kept_ids` only — the source endpoint is the language that emitted the edge. Bare-name `to_id` targets stay accessible because the rule does not require `to_id` membership; resolver promotes unresolved bare names to Dangling. Two regression tests landed: `pre_filter_excludes_other_language_edge_pointing_at_kept_symbol` (the codex bypass) + `pre_filter_keeps_target_language_edge_with_bare_name_to` (complement: bare names must still pass). `cargo test -p scope-core` = 85 passed (was 83).
- 2026-05-12 | sprint 0005 codex review (round 2) | clean | branch refactor/sprint-0005-framework-infrastructure | notes: codex round-2 review with model upgrade — `codex review -c model="gpt-5.3-codex" -c model_reasoning_effort="high" --base main --title "sprint 0005 — R5 round 2"`. Verdict: clean. Codex final report: "I did not identify any actionable regressions in the introduced framework infrastructure, dispatch logic, CI gates, or integration tests relative to the merge base. The new code compiles and targeted tests pass, and the changes appear internally consistent with the stated R5 scope." Zero P0 / P1 / P2 / P3 findings. Review focus per `sprints/0005 § Reporting`: R5 acceptance bullets, E2 mechanical enforcement (graph-only via metadata), DetectedVersion / ResolvedVersion / UnknownVersionPolicy correctness, cross-language pre-filter, the two CI gates this sprint activates. Sprint 0005 closes; single-sprint phase, so close commit also flips Phase C to shipped.
- 2026-05-12 | R5 | in-progress → shipped + Phase C | in-progress → shipped | commit 4b8fec9 | notes: single-sprint phase atomic close. `cargo test --workspace` = 530 passed (528 from first-impl + 2 dispatch regression tests). `just gate-refactor` = 12 active CI gates green: ci-edge-sealed, test-builder, test-typestate, ci-context-shape, ci-no-fs, ci-dispatch, ci-trait-shape, ci-no-spawn, ci-no-network, ci-immutable, ci-no-framework-scm, ci-patterns. Class-3 (discipline-only) surface now contains only B1 (flow analysis) + E3 (heuristic optimization); every other rule on every active language plugin + framework plugin has a mechanical safety net. Sprint 0006 (Phase D / R10 + R8 — typed output schema + confidence audit) cuts from post-merge `main`.
- 2026-05-12 | sprint 0006 ambiguities resolved on main | four-of-four resolved by ARCHITECTURAL-REFACTOR.md § R8 amendment + sprint 0006 doc citations | commit cdf24bb | notes: per `sprints/README.md § 3 ambiguity protocol`. (1) reference fixture corpus path locked at `gumiho-mudang-scope/scope-core/tests/fixtures/reference/<language_slug>/` with one subtree per supported `LanguageId`; provenance is real anonymized maintainer snippets per `LANGUAGE-PLAYBOOK.md` Step 5; anonymization rules — strip secrets + shape-preserving placeholder substitution — recorded in `tests/fixtures/reference/README.md` (authored alongside first fixture). (2) sampling protocol: default `N = 30` per `(kind, confidence)` cell, `--sample-size N` override, `--seed N` for reproducibility (default fixed compile-time constant); 30 chosen as binomial-proportion lower bound (±18-percentage-point CI half-width at p=0.95) — sufficient for early tier-drift signal, the maintainer tightens with `--sample-size` when suspicion exists. (3) labelling channel: manual is the default and only channel R8 ships; two-phase workflow `--emit-sample <path>` → maintainer fills `label` slot → `--label <path>`; LLM-assisted labelling (`--labeller <executable>`) deferred post-refactor per `POST-REFACTOR-PLAN.md` § Items deliberately deferred (LLM labeller is itself a precision source and demands its own audit). (4) output format: default `--format json` with top-level `schema_version: "1"` + `report` array of `(kind, tier, producer, pattern_id, sample_size, correct_count, precision)` rows; `--format tsv` for shell pipelines (same columns); JSON shape is the contract. Sprint 0006 branch (`refactor/sprint-0006-output-audit`) cuts from this commit on main.
- 2026-05-12 | POST-REFACTOR-PLAN Priority 2 queued on main | honesty audit + R0 args_text 2 KB cap removal | commit 60a552e | notes: per `sprints/README.md § 3 ambiguity protocol`. Surfaced mid-sprint-0007 (during R8 chunk 4 design): the R0 `edges.args_text` 2 KB cap + `[truncated]` marker (`scope-core/src/edge.rs` const `ARGS_TEXT_CAP_BYTES = 2048`) is an approximation justified by *"common case fits"* rather than a hard runtime constraint (panic / OOM / won't-run). A code analyser must never lie about its data; this cap silently corrupts every consumer of `args_text` (resolver, framework plugins, future LSP integration, future time-travel queries). New POST-REFACTOR-PLAN.md § Priority 2 — Honesty audit codifies the principle (Scope may be slower on pathological input but must never be inaccurate) and queues five sub-items: (a) charter-grade audit of every doc rationale for non-essential approximations, (b) code-grade audit of every `CAP` / `LIMIT` / `MAX` / `TRUNC` / `BUDGET` / `THRESHOLD` constant, (c) drop the R0 `args_text` 2 KB cap (charter-grade amendment + wipe-and-reindex per CHARTER §2), (d) fix any further offenders found by (a)+(b), (e) capture remaining justified approximations as explicit invariants. Cross-link added at ARCHITECTURAL-REFACTOR.md § R0 → Mitigation 2 stating the slated removal and reason. Sprint 0007 R8 sidesteps the issue by design — `source_snippet` is read from the source file at audit time, not from `args_text` — so chunk 4 + remaining R8 chunks proceed unchanged. refactor/phase-d and refactor/sprint-0007-confidence-audit rebase onto this commit before R8 chunk 4 resumes.
- 2026-05-12 | AUDIT-LABEL-SCHEMA § Auditor immutability rule landed on main | hard SHA-256 drift gate promoted from Priority 2 to sprint 0007 chunk 4 | commit c594421 | notes: per `sprints/README.md § 3 ambiguity protocol`. Surfaced mid-sprint-0007 during R8 chunk 4 design: "R8 measures the extractor's accuracy at a snapshot in time" demands the indexed graph and the source files together be frozen during audit. Editing source between `--emit-sample` and `--label` invalidates the measurement (labeller judges against source the extractor never saw). New `docs/AUDIT-LABEL-SCHEMA.md` § Auditor immutability rule documents the rule **mechanically**: both `--emit-sample` and `--label` re-compute SHA-256 of every source file referenced by the sampled / labelled edges and compare against `file_hashes.hash` (the indexer's index-time hash, already stored per R0 schema); any mismatch (different content, deleted file) is a hard error with re-index remediation. **No `--allow-drift` flag** — searched four scenarios (archaeology, CI lag, performance, mtime-without-content drift) and found no legitimate audit use case for stale-vs-current comparison; mtime-without-content is handled correctly by SHA-256 (hash matches, audit proceeds). Promotes the hard mechanical lock from a Priority 2 future-enhancement to a chunk 4 sprint 0007 deliverable so the auditor-immutability rule is enforced by code from R8's first ship, not by maintainer discipline. POST-REFACTOR Priority 2 sub-item (c) updated to bundle the args_text cap drop with a JSONL `schema_version` bump `"1"` → `"2"` adding a `producer_captured_args: string | null` field — the two fidelity moves ship as one charter-grade amendment because dropping the cap is what makes the new field actually faithful. refactor/phase-d and refactor/sprint-0007-confidence-audit rebase onto this commit before R8 chunk 4 resumes.
- 2026-05-12 | R8 | unstarted → in-progress (later **reverted to unstarted** when Phase D was split mid-sprint — see re-scope log entry below) | branch refactor/sprint-0006-output-audit (off main, commit cdf24bb; branch renamed to `refactor/sprint-0006-typed-output-schema` after the split) | notes: sprint 0006 originally opened claiming both R8 + R10. Phase D was originally scoped as a single atomic sprint. The mid-sprint re-scope (see next-to-last log entry below) moved R8 to sprint 0007.
- 2026-05-12 | R10 | unstarted → in-progress | branch refactor/sprint-0006-typed-output-schema (off main, commit cdf24bb; branch renamed mid-sprint from `refactor/sprint-0006-output-audit` after Phase D re-scope) | notes: sprint 0006 opened. Typed output structs replace string-concatenation formatters in `gumiho-mudang-cli/src/commands/`; output schema audit (`scripts/audit_output_schema.sh` → `just ci-output-schema`) lands in same sprint as field-shape mechanical enforcement of E1 (no `error` / `warning` / `diagnostic` / `is_valid` / `lint` / `correctness` field names anywhere in the output struct surface).
- 2026-05-12 | sprint 0006 mid-sprint scope decision | R10 strict reading locked | commit 47f3bf7 | notes: mid-sprint scope decision recorded before R10 implementation begins. The R10 target-state phrase *"Formatters serialize structs; they do not concatenate strings"* is read **strictly** — every output renderer in `gumiho-mudang-cli` (the four sprint-plan-named formats `sketch` / `summary` / `compact` / `json` plus per-command surfaces `refs` / `deps` / `impact` / `trace` / `flow` / `entrypoints` / `status` / `workspace` / `map` / `find` / `similar` / `source` / `diff`) converts to a `#[derive(Serialize)]` struct or enum at the output boundary. Procedural `println!` / `format!` collapses into `impl Display` on the typed output struct. `serde_json::json!()` ad-hoc tree construction is removed; every JSON-emitting path serializes a concrete typed value. Rationale: (1) maximises "make illegal states unrepresentable" — sum types over render variants prevent constructing a `ClassSketch`-shaped output for a method-shaped symbol; (2) `#[derive(Serialize)]` streams to the writer with no `serde_json::Value` intermediate (faster after LTO release-fast inlines the monomorphized impls); (3) opens the door to future LSP composition consuming Scope output via shared Rust types or `ts-rs` / `typeshare` bindings (forward-looking note added at `gumiho-mudang-lsp/docs/SCOPE_OUTPUT_INTEROP.md`). Costs accepted: ~3-5 days work; all 28 `insta` snapshot tests re-anchor; ~50 KB extra binary from serde-derive monomorphization. Amendment landed in `ARCHITECTURAL-REFACTOR.md § R10 → Sprint 0006 scope decision` + `sprints/0006-phase-d-typed-output-schema.md § R10 scope decision (locked)` + `gumiho-mudang-lsp/docs/SCOPE_OUTPUT_INTEROP.md` (new doc).
- 2026-05-12 | Phase D re-scope mid-sprint | single-sprint → two-sprint with integration branch (precedent: Phase B) | commit 8fb5e64 | notes: discovered post-scope-lock that R10 strict reading alone is a 3-5 day refactor across ~14 CLI commands + a 1477-LOC `formatter.rs`, with 28 `insta` snapshots to re-anchor — and R8 (subcommand + reference corpus + sampling + manual-label pipeline + JSON/TSV report writer + new CI gate) is independently substantial. Combined scope did not fit one sprint without forcing partial deliverables. Per `sprints/README.md § 1` — atomic phase shipment is preserved at the `main`-branch level via phase-integration branches — Phase D adopts the Phase B precedent: two sprints onto `refactor/phase-d`, then a single phase-close merge to `main`. Sprint 0006 ships R10 only (renamed to `0006-phase-d-typed-output-schema.md`; branch renamed to `refactor/sprint-0006-typed-output-schema`). Sprint 0007 ships R8 only (new file `0007-phase-d-confidence-audit.md`; branch `refactor/sprint-0007-confidence-audit` cuts from `refactor/phase-d` after sprint 0006 merges). Existing Phase E sprints renumbered: previous 0007 (malformed harness) → 0008; previous 0008 (charter sweep) → 0009. `sprints/README.md` sprint table, branch-naming examples, multi-sprint lifecycle diagram, and refactor-as-five-phase-closes summary all updated. R8's status reverts `in-progress → unstarted` (it lives in sprint 0007, not started yet). R10 stays `in-progress` on the renamed branch. Phase D row notes record two-sprint shape.
- 2026-05-12 | sprint 0007 opened | R8 unstarted → in-progress | branch refactor/sprint-0007-confidence-audit (off refactor/phase-d, HEAD c034f2a after main amendment 56780b2 rebase) | notes: Phase D second sprint. Four pre-Phase-D ambiguities (#1 reference fixture corpus path, #2 sampling protocol, #3 manual-labelling pipeline, #4 JSON/TSV output shape) were resolved on `main` in commit `cdf24bb` ahead of sprint 0006. Sprint 0007 extends #3 + #4 on `main` in commit `56780b2` (pre-code, §3 ambiguity protocol): #3 locks JSONL sample file as the plug-point contract for any external labeller (LLM / LSP cross-check / hybrid / human); #4 locks JSONL sample-row schema at `schema_version: "1"` with reserved `lang_version: Option<String>` slot (always null in this sprint). New doc `docs/AUDIT-LABEL-SCHEMA.md` carries the full contract. Self-correction cycle queued as Priority 1 in POST-REFACTOR-PLAN.md (continuous re-audit, labeller crates ecosystem, ML-driven extractor patch suggester, lang_version detector matrix across all 7 languages atomic). Sprint-close commit will atomically flip R8 → shipped + Phase D row → shipped + merge sprint-0007 → refactor/phase-d → main (Phase D atomic close per `sprints/README.md § 1` Phase B precedent).
- 2026-05-12 | R10 chunk-by-chunk delivery | sprint 0006 implementation log | commits 689694c, 46a9159, 38247ca, 9812109, ab0b667, a73c7f7, aef3a3c, a3f372e, 13b5fad, f5d086a, 2f434d7 | notes: R10 strict reading shipped in eleven incremental commits on `refactor/sprint-0006-typed-output-schema`. (1) Chunk 1 `689694c` — schema module scaffold under `gumiho-mudang-cli/src/output/schema/` (`compact_symbol.rs`, `symbol_sketch.rs`, `edge_summary.rs`); `JsonOutput<T: Serialize>` envelope already generic before the sprint, no envelope change needed. (2) Chunk a `46a9159` — `commands/sketch.rs` ports nineteen `serde_json::json!()` sites to a `SymbolSketch<'a>` sum-type with six tagged variants (Class/Method/Interface/Enum/Generic/File) plus the borrowed `CompactSymbol<'a>` for `--compact`. Two scaffold bugs caught at this stage and patched into chunk a: `MethodSketch.calls` and `InterfaceSketch.implementors` are `&'a [String]` (matching `graph.get_outgoing_calls` / `get_implementors` shapes), not `&'a [Symbol]`. (3) Chunk b `38247ca` — `commands/summary.rs` ports two `json!()` sites to `Summary<'a>` sum-type tagged `summary_kind`. (4) Chunk c `9812109` — `commands/refs.rs` ports two `json!()` sites (grouped class-refs envelope + empty-workspace placeholder) to `RefsGrouped<'a>` envelope; `deps.rs` + `impact.rs` audit-confirmed already typed pre-sprint. `EdgeSummary` sum-type dropped at this point (YAGNI: the three concrete edge types already derive `Serialize` and the commands pass them through directly; re-introduce when a downstream consumer needs edge-uniform handling — original scaffold preserved at commit `689694c`). (5) Chunk d `ab0b667` — `commands/index.rs` ports seven `json!()` sites covering both single-shot output (`IndexFullResult`, `IndexIncrementalUpToDate`, `IndexIncrementalResult`, `IndexLanguageStat`) and the `--watch --json` event stream (`IndexEvent` sum-type tagged `event` with `Start`/`Reindex`/`Stop` variants). Index keeps its bespoke top-level shape (`command`, `mode`, `event` keys) rather than being forced through `JsonOutput<T>`; envelope unification is not what R10 mandates. (6) Chunk e `a73c7f7` — `commands/source.rs`, `init.rs`, `setup.rs` port their single `json!()` site each (`SourceView<'a>` / `InitResult<'a>` / `SetupResult`). After this commit the CLI has **zero** `serde_json::json!()` sites in `commands/` or `output/` — R10 strict-reading bullet 3 met. (7) Chunks g.A `aef3a3c`, g.B `a3f372e`, g.C `13b5fad`, g.D `f5d086a`, g.E `2f434d7` — `output/formatter.rs` end-to-end Display collapse: every `pub fn print_*` free function (23 in total) replaced by an `impl fmt::Display` on a typed view struct (`ClassSketchView`, `MethodSketchView`, `InterfaceSketchView`, `EnumSketchView`, `GenericSketchView`, `FileSketchView`, `RefsView`, `RefsGroupedView`, `FileRefsView`, `WorkspaceRefsView`, `DepsView`, `FileDepsView`, `ImpactView`, `TraceView`, `FlowView`, `EntrypointsView`, `MapView`, `IncrementalResultView`, `FindResultsView`, `StatusView`, `WorkspaceListView`, `WorkspaceStatusView`, `WorkspaceFindResultsView`). Private helpers `print_header` → `write_header(symbol, f)`, `print_snippet_context` → `write_snippet_context(...,  f)`; helpers that return `String` (`method_display_line`, `extract_enrichment_prefix`, `extract_modifiers`, `format_number`, `truncate_str`, `humanize_edge_kind`, `impact_depth_label`) stayed unchanged and are called from the Display bodies. Renderer bodies translated `println!(...)` → `writeln!(f, ...)?` verbatim; behaviour preserved byte-for-byte (insta snapshots match without `.new` files; 13 `.snap` files unchanged after Display conversion, not the 28 estimated pre-implementation). Callers in `commands/*.rs` switch from `formatter::print_X(args)` to `print!("{}", formatter::XView { ... })` (or `eprint!` for `IncrementalResultView` which preserves the original stderr destination). After chunk g.E `formatter.rs` has **zero** `println!` / `eprintln!` macro sites and **zero** `pub fn print_*` free functions — R10 strict-reading bullet 2 met. (8) All 13 `gate-refactor` gates green throughout, including `ci-output-schema` (active since chunk 1). `cargo test --workspace` green per maintainer run.
- 2026-05-12 | phase-d + sprint-0007 rebased onto main 60a552e | post-Priority-2 amendment SHA refresh | branch refactor/phase-d HEAD 92b6147; branch refactor/sprint-0007-confidence-audit HEAD aeda955 | notes: `main` amended at `60a552e` (POST-REFACTOR Priority 2 queueing) per §3 ambiguity protocol. `refactor/phase-d` rebased onto new main; conflict in `REFACTOR-STATUS.md` resolved by preserving both the new Priority 2 log entry and the existing Phase D re-scope chain. R10 chunk SHAs refreshed by this rebase: chunk 1 `689694c → e164664`, chunk a `46a9159 → dda255b`, chunk b `38247ca → b0181c8`, chunk c `9812109 → ee69ad6`, chunk d `ab0b667 → f1fe331`, chunk e `a73c7f7 → 32baded`, chunk g.A `aef3a3c → 0623c17`, chunk g.B `a3f372e → b845015`, chunk g.C `13b5fad → d258820`, chunk g.D `f5d086a → 6c4178a`, chunk g.E `2f434d7 → c283cbd`; sprint-close commit `01c7c0c → ebe3285`; codex P1 follow-up `c034f2a → 92b6147`. `refactor/sprint-0007-confidence-audit` reset to new phase-d HEAD `92b6147` and cherry-picked in correct chronological order: open-sprint-0007 `8b992f7 → 442883d`, reflect-56780b2-in-sprint-0007-doc `6a78f0b → 0fa1910`, R8 chunk 1 `8b0e319 → 1f3360c`, chunk 2 `41e17fb → ac926c0`, chunk 3 `097c616 → aeda955`. The two prior paper-trail commits `e6e534e` (sprint 0006 SHA mapping post-rebase) and `1b64c86` (R8 row branch-HEAD reference fix) were not re-applied — they recorded drift from the prior rebase and would re-introduce stale SHAs; this log entry supersedes them with the current SHA mapping. `cargo check --workspace` green; `cargo test commands::audit::tests` = 7 passed (R8 chunk-3 sampler tests intact). R8 row in § Moves updated to record the new `refactor/phase-d` HEAD `92b6147` and the amendment trigger.
- 2026-05-12 | phase-d + sprint-0007 rebased onto main c594421 | post-auditor-immutability-rule SHA refresh | branch refactor/phase-d HEAD 4813fa4; branch refactor/sprint-0007-confidence-audit HEAD 00d0e62 | notes: `main` amended at `c594421` (auditor immutability rule + hard SHA-256 drift gate promoted to chunk 4) per §3 ambiguity protocol. `refactor/phase-d` rebased onto new main; `REFACTOR-STATUS.md` conflicts resolved (kept new auditor-immutability log entry + Phase D re-scope chain). R10 chunks re-shifted: chunk 1 `e164664 → c1bcdab`, chunk a `dda255b → 5d33bef`, chunk b `b0181c8 → 88e02c1`, chunk c `ee69ad6 → d3a0a39`, chunk d `f1fe331 → 7099bbe`, chunk e `32baded → 6f56cc0`, chunk g.A `0623c17 → 92619b3`, chunk g.B `b845015 → 7af6d70`, chunk g.C `d258820 → 6c1ee27`, chunk g.D `6c4178a → 95a4be7`, chunk g.E `c283cbd → 70f212c`; sprint-close `ebe3285 → 25a5e9c`; codex P1 `92b6147 → 4813fa4`. `refactor/sprint-0007-confidence-audit` reset to new phase-d HEAD `4813fa4` and cherry-picked: open-sprint-0007 `442883d → 50e2705`, reflect-56780b2 `0fa1910 → d0915e2`, chunk 1 `1f3360c → e7ecbe9`, chunk 2 `ac926c0 → b8be78c`, chunk 3 `aeda955 → a320a33`, prior-rebase-paper-trail `f7e2c83 → 7254c7b` (cherry-picked verbatim — its content cites SHAs from the previous rebase and is preserved as historical record; this new log entry supersedes it). R8 row in § Moves refreshed to record `refactor/phase-d` HEAD `4813fa4` and amendment trigger `c594421`. `cargo check --workspace` green; `cargo test commands::audit::tests` = 7 passed. Chunks 1-3 R8 implementation intact, ready for chunk 4 to start.
- 2026-05-12 | R8 chunks 4-7 delivery | sprint 0007 implementation log + Confidence audit CI gate flipped `planned (Phase D)` → `active (sprint 0007 — 2026-05-12)` | commits fd4110f, cd1ce44, 810570b, 63fbaaa | notes: R8 implementation lands in four incremental commits on `refactor/sprint-0007-confidence-audit`. (1) Chunk 4 `fd4110f` — two-phase labelling. New `--emit-sample <PATH>` writes JSONL per `docs/AUDIT-LABEL-SCHEMA.md` (schema_version "1"); `--label <PATH>` reads it back. Hard SHA-256 drift gate via new `Graph::check_audit_freshness(project_root, file_paths)` re-hashes every file referenced by sample / labelled records and compares against `file_hashes.hash` (the indexer's index-time digest, byte-identical via shared `sha2` + `hex` crates). Any mismatch — modified / missing / unknown-to-index — aborts the audit with re-index remediation; no `--allow-drift` escape per `AUDIT-LABEL-SCHEMA.md` § Auditor immutability rule. Source snippets are read from the on-disk file (`read_source_snippet` reads line N 1-based per `parser.rs::start_position().row + 1` convention) — `args_text` is never touched, per POST-REFACTOR-PLAN.md § Priority 2 sub-item (c) (the 2 KB cap on that column is queued for charter-grade removal bundled with schema bump "1" → "2" adding `producer_captured_args`). 5 unit tests (SampleRecord serde + schema_version constant lock) + 5 integration tests (JSONL shape, drift gate, schema reject, mutex). (2) Chunk 5 `cd1ce44` — precision report + JSON / TSV writers. `compute_precision_report(records)` groups by `(kind, tier, producer, pattern_id)` via BTreeMap (sorted iteration; deterministic output). `PrecisionReport { schema_version, disclaimer, report }` typed shape; rows are `(kind, tier, producer, pattern_id, sample_size, correct_count, precision)` per pre-Phase-D ambiguity #4 contract. `--format <json|tsv>` flag, default json (pretty-printed via `serde_json::to_writer_pretty` — no intermediate `Value`); TSV header + tab-separated rows with 4-decimal precision (pinned by `write_report_tsv_uses_four_decimal_precision` so the 0.95 / 0.9499 boundary stays distinguishable). Null-label rejection: `--label` bails if any record has `label=null` — mixing labelled + unlabelled rows in one report would mis-state the denominator, the honest path is mechanical refusal until labelling completes. 6 unit tests + 3 integration tests (replaced the chunk-4 "chunk-5-bail" assertion with three end-to-end shape tests). (3) Chunk 6 `810570b` — tier gate. `check_tier_gate(report)` enforces `HIGH_TIER_MIN = 0.95` / `MEDIUM_TIER_MIN = 0.70` / low no minimum per R8 acceptance. Collects **every** offender (not just first) — operator needs the full failing-pattern list to choose between downgrading the stamp or fixing the pattern. Unknown tier strings hard-fail rather than silently pass. Order: `write_report` to stdout, then gate runs — report stays visible on stdout regardless of exit code, gate verdict + remediation go to stderr. 7 unit tests + 1 integration test (`test_audit_confidence_tier_gate_fails_on_low_precision_high_tier` labels every record `false`, asserts failure exit + JSON on stdout + tier-gate error with target percentages + remediation on stderr). (4) Chunk 7 `63fbaaa` — `just audit-confidence` recipe + CI gate flip. The recipe is `cargo test -p gumiho-mudang-cli --test test_audit_confidence`, not an end-to-end labelled-sample replay. Reframing rationale (resolved on-branch mid-chunk-7 by maintainer): the mid-refactor CI gate's job is **mechanical regression prevention** (wiring break of the subcommand, `SampleRecord` field-order drift breaking external labellers, drift-gate removal, tier-gate threshold loosening, schema_version bump without contract update), not driving labelled samples through CI. The continuous re-audit cycle — committed labelled samples + edge_id-stable join key + precision-drift detection over time — is post-refactor work and stays at `POST-REFACTOR-PLAN.md` § Priority 1, where the cross-reindex join key is also designed (R0 surrogate edge_ids do not survive a re-index; replay needs a stable `(from, to, kind, pattern_id)` join). `CI-GATES.md` row 29 flipped `planned (Phase D)` → `active (sprint 0007 — 2026-05-12)` with description rewritten to reflect this mid-refactor scope. `just gate-refactor` aggregator extended to 14 active gates. `just audit-confidence` green (all 8 integration tests pass in 8.21s). `cargo test commands::audit::tests` = 25 passed; `cargo test --test test_audit_confidence` = 8 passed; `cargo check --workspace` + `cargo clippy --workspace --tests --no-deps` both green. Sprint 0007 DOD #5 ("first run of `scope audit confidence` against the reference corpus produces a report that the human reviews") remains an explicit maintainer task — independent of the CI gate; the gate enforces mechanical regression invariants, the first-run review is a human design checkpoint that may land in the sprint-close commit or be performed separately at maintainer discretion.
- 2026-05-12 | R8 chunk 8 delivery | sprint 0007 implementation log — AUDIT-LABEL-SCHEMA.md pointer wired into report header (both JSON + TSV surfaces) | commit 0807b7c | notes: chunk 4's `--help` text already cited `docs/AUDIT-LABEL-SCHEMA.md` on the `--emit-sample` flag (covering one half of the sprint deliverable "AUDIT-LABEL-SCHEMA.md referenced from --help text + report header"). Chunk 8 closes the other half: `PrecisionReport` gains a top-level `sample_schema_doc: String` field populated from the pre-existing `SCHEMA_DOC_POINTER` const ("Sample-file schema: docs/AUDIT-LABEL-SCHEMA.md (schema_version \"1\")."); JSON output emits the field after `disclaimer`; TSV output gains a 2-line `#`-prefixed preamble (disclaimer + schema-doc pointer) before the column header. Both surfaces carry the same pointer so external labeller authors (LLM / LSP / hybrid) discover the contract from either format. `#` prefix matches the convention standard TSV consumers (awk / Miller / csvkit) recognise as a comment. 3 new unit tests + 1 new integration test + 1 updated unit test + 1 updated integration test. `cargo test commands::audit::tests` = 28 passed; `cargo test --test test_audit_confidence` = 9 passed; `just audit-confidence` green; `just gate-refactor` = 14 active gates green.
- 2026-05-12 | POST-REFACTOR-PLAN Priority 3 queued | layering audit — thin CLI, fat library | commit 1c4a011 | notes: surfaced at sprint 0007 close (mid-chunk-10 by maintainer): `gumiho-mudang-cli/src/commands/audit.rs` grew to ~1400 LOC across chunks 3-8, and the bulk of it is library-grade — sampling engine + PRNG, schema-versioned wire formats (`SampleRecord` / `PrecisionReport` / `ReportRow` / `ReportFormat`), precision math, report writers (JSON + TSV), tier gate (`check_tier_gate` + `HIGH_TIER_MIN` / `MEDIUM_TIER_MIN`), auditor-immutability machinery. None of it belongs in the CLI's presentation layer; all of it belongs in a `scope-audit` sub-crate so future hosts (LSP, web service, batch CI) can invoke R8 without taking `gumiho-mudang-cli` as a dependency (which would invert charter §4 layering). Priority 3 codifies a layering-audit principle (CLI = clap surface + dispatch + output formatting against the R10 typed schema; analysis algorithms, wire formats, and mechanical invariants live in scope sub-crates) and queues the `commands/audit.rs` extraction as its known offender. Sub-items: (a) cut `scope-audit` sibling sub-crate, migrate sampling / wire-format types / precision compute / writers / tier gate / immutability surface; (b) decide sub-crate-vs-module — sibling cleaner because `scope-audit` wants `scope-graph` types and an upward `scope-core → scope-graph` dep does not exist today; (c) reduce `commands/audit.rs` to dispatch only (target < 200 LOC); (d) migrate inline unit-test block to `scope-audit` (integration suite stays CLI-side); (e) audit every other `commands/*.rs` for the same offender pattern — spot-check candidates at sprint close: `index.rs` (large indexer driver), `flow.rs` / `trace.rs` (graph traversal lives in the CLI), `setup.rs` (process spawn lives in the CLI); triage and queue, do not bundle into one sprint. Bundled into the sprint-0007 close commit rather than a separate `main` amendment because Priority 3 is a TODO recording — it does not gate code, does not retroactively change any R-move acceptance bullet, and ships to `main` naturally via the Phase D atomic close (sprint 0007 → refactor/phase-d → main).
- 2026-05-12 | sprint 0007 close + Phase D atomic close | R8 in-progress → shipped + Phase D in-progress → shipped | commit 1c4a011 | notes: sprint 0007 atomically closes Phase D. § Moves R8 row flipped `in-progress → shipped`; § Phase snapshot D row flipped `in-progress → shipped` with Shipped date 2026-05-12. R8 acceptance bullets demonstrated across chunks 1-8 on `refactor/sprint-0007-confidence-audit` — see the R8 § Moves row notes and the prior chunk-by-chunk log entries. CI gate row 29 (Confidence audit) flipped `planned (Phase D) → active (sprint 0007 — 2026-05-12)` in chunk 7. `just gate-refactor` = 14 active gates green (the 13 from sprint 0006 + audit-confidence). `cargo test commands::audit::tests` = 28 passed; `cargo test --test test_audit_confidence` = 9 passed; `cargo check --workspace` + `cargo clippy --workspace --tests --no-deps` both green. POST-REFACTOR-PLAN.md Priority 3 (layering audit) landed in the same commit as a queued post-refactor TODO (see preceding log entry). Sprint plan reporting Merge step: sprint 0007 close commit will merge to `refactor/phase-d`; `refactor/phase-d` will then merge to `main` (Phase D atomic close per `sprints/README.md § 1` Phase B precedent). Maintainer-triggered codex review runs against this close commit per sprint plan § Reporting → Codex review (canonical command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base refactor/phase-d --title "sprint 0007 — R8"`); the merge to main follows the review verdict.
- 2026-05-12 | sprint 0007 codex review chain | eight rounds, all P-level findings closed | commits 5b80e25, 8493c25, 37b5564, 4f38adf, 28c1474, 8d75db6, 60264ef, 9d865a8, f9d2057 (on `refactor/sprint-0007-confidence-audit`) | notes: post-close codex review chain per `sprints/README.md § 9 — Role 1`. Maintainer used the high-reasoning variant from the start (`codex review -c model="gpt-5.3-codex" -c model_reasoning_effort="high" --base refactor/phase-d --title "sprint 0007 — R8 round N"`). Each round surfaced a real, distinct finding on the audit subcommand contract; the chain ran eight rounds before maintainer closed it per the sprint-0004 diminishing-returns precedent. Round-by-round verdicts: **R1** `5b80e25` (P1 edge_id integrity gate — non-parseable / unknown-to-index ids silently bypassed drift gate while still entering precision math); **R2** `8493c25` (P2 sample-vs-index tamper gate on the four report-key fields kind / confidence / producer / pattern_id — labeller could downgrade `confidence: high → low` and pass tier gate); **R3** `37b5564` (three P2 findings closed in one commit — refuse to overwrite at `--emit-sample` destination via `create_new(true)`, tolerate documented partial labelling by skipping `label=null` records before group accumulation, reject duplicate edge_ids that previously double-counted in the report); **R4** `28c1474` (P2 tamper gate extended to from / to / source_snippet — labeller rewriting any of these judged a different edge / context than the report would credit); **R5** `8d75db6` (P2 drift gate moved before tamper gate so source-file edits surface as drift with `scope index` remediation rather than mis-diagnosed as sample tamper; `unwrap_or_default()` replaced with error propagation; P3 default no-flag `scope audit confidence` invocation rewritten as a successful introspection surface — the stale chunk-plan bail pointer is gone); **R6** `60264ef` (P2 records missing the `label` JSON key now rejected at parse boundary — distinct from explicit `label: null` which remains a valid skip per partial-coverage); **R7** `9d865a8` (P2 tamper gate closes the `lang_version` axis — sprint 0007 always emits `null`, any non-`None` value is tamper); **R8** `f9d2057` (P3 key-presence pre-check generalised to every required schema_version "1" field, replacing the round-6 label-specific check with a constant listing all 11 required fields; surfaces all missing keys at once). Final coverage matrix: every field on the sample-record schema has BOTH parse-time presence enforcement AND post-parse value enforcement — schema_version (presence + schema check), edge_id (presence + integrity gate parse/resolve/dup), eight tamper-gate fields (presence + value match against indexed row / re-derived snippet), `label` (presence + typed value validation). The schema-doc invariant "labeller fills `label` only" is now mechanically enforced in code, not procedurally requested. Verification at chain close: `cargo test commands::audit::tests` = 30 passed; `cargo test --test test_audit_confidence` = 23 passed (8 starting tests + 15 round-by-round regressions); `cargo check --workspace` + `cargo clippy --workspace --tests --no-deps` both green; `just audit-confidence` green; `just gate-refactor` = 14 active gates green. Chain closed per maintainer call — round 9 would have to look at orthogonal surfaces (sampling PRNG determinism, write_report shape, tier_gate boundaries, emit-sample edge cases, file-hash race conditions); diminishing-returns threshold reached. Sprint 0007 ready to merge `refactor/sprint-0007-confidence-audit` → `refactor/phase-d` → `main` (Phase D atomic close per `sprints/README.md § 1` Phase B precedent).
- 2026-05-12 | sprint 0007 close + Phase D atomic close landed on main | merge commits d83111d (sprint-0007 → phase-d) + 9e9732c (phase-d → main) | branch refactor/phase-d HEAD e750580 absorbed into main via `--no-ff` merge | notes: per `sprints/README.md § 5 — Branch protocol`. Sprint 0007 close commit `1c4a011` plus 9 codex-fix commits + paper-trail commit `e750580` merged to `refactor/phase-d` via `--no-ff` (merge commit `d83111d`, parents `4813fa4` + `e750580`); `refactor/phase-d` then merged to `main` via `--no-ff` (merge commit `9e9732c`, parents `c594421` + `d83111d`). Tree at `9e9732c` = tree at `e750580` (TREESAME merge — `git log` default history simplification suppresses the merge commit display; `git rev-list HEAD` confirms it exists with correct two parents). Phase D delta on main: 74 files, +5658/-1417. `just gate-refactor` post-merge = 14 active gates green; 30 audit unit + 23 audit integration tests pass. No push to remote performed.
- 2026-05-12 | post-Phase-D doc cleanup | redistribute Phase E sprint DoDs + sweep stale 0007/0008 refs | commit 4522335 | notes: per `sprints/README.md § 3 ambiguity protocol`. Phase D split commit `8fb5e64` did `git mv` on sprint files but did not redistribute content. Two passes landed: (Pass 1 — mechanical) replaced 12 `<this-commit>` placeholders with actual SHAs (Phase B close `5a68103`, snapshot cleanup `5077d0d`, Phase C close `4b8fec9`, R10 strict reading lock `47f3bf7`, Phase D re-scope `8fb5e64`, R8 chunk 7 `63fbaaa`, Priority 3 + sprint 0007 close `1c4a011`, Phase D row + R8 row + sprint-0007 rebase HEAD `00d0e62`, R10 row `25a5e9c`); flipped Phase E row notes "0007/0008" → "0008/0009"; flipped 5 compat-shim rows "sprint 0008 (Phase E close)" → "sprint 0009 (Phase E close)"; corrected sprint 0008 H1 header (was "Sprint 0009"), body refs, branch name + codex titles; bumped sprint 0009 prerequisite "Sprints 0001–0007" → "0001–0008"; updated docs/README.md Phase D status. (Pass 2 — DoD redistribution) sprint 0008 DoD tightened to R6-only (R6 deliverables + ambiguities resolved + malformed-source CI gate + snapshot tests + R6 row flip with Phase E staying in-progress + sprint-scope codex clean); sprint 0009 § Acceptance absorbed full-refactor criteria (R0–R12 + A–E shipped, class-1/2/3 coverage, language-plugin NEEDS REVIEW = 0, framework forward-looking, < 10% benchmark regression, scope audit confidence against fixture corpus, malformed/typed-trait/immutable-source gates in CI, sprints 0001–0008 acceptance preserved, POST-REFACTOR eligibility); new § Reporting section added to sprint 0009 mirroring sprint 0008 structure; chunk 4 expanded to call out both codex passes (sprint-scope `gpt-5.5 medium` + full-refactor-scope `model_reasoning_effort=high` override).
- 2026-05-12 | sprint 0008 pre-branch ambiguities resolved on main | three-of-three resolved by ARCHITECTURAL-REFACTOR.md § R6 amendment | commit d99d11c | notes: per `sprints/README.md § 3 ambiguity protocol`. (1) Fixture provenance + placement — hand-crafted synthetic (matches R6 "deliberately broken sources" phrasing) at `gumiho-mudang-scope/scope-core/tests/fixtures/malformed/<language_slug>/<case>/` mirroring R8 `reference/` scaffold; per-fixture dir carries broken source + `expected.md` pinning the human-readable skipped-range expectation. (2) Per-language category coverage — 5-fixture floor mechanical; category selection per language's grammar (JSX missing-close-tag is TS/TSX-only; Python no braces → "broken indent" / "EOF mid-block" fills the slot); categories documented in each fixture's `expected.md`. (3) Snapshot tool — `insta` confirmed (already workspace dep); sprint 0008 adds `insta = { workspace = true }` to scope-core dev-deps. Sprint 0008 branch (`refactor/sprint-0008-malformed-harness`) cuts from this commit on main.
- 2026-05-12 | R6 | unstarted → in-progress | branch refactor/sprint-0008-malformed-harness (off main, commit d99d11c) | notes: sprint 0008 opened. Phase E first sprint; sprint 0009 (charter sweep) follows to close the refactor. Sprint-close commit will flip R6 → shipped; Phase E row stays `in-progress` until sprint 0009 closes per the renumbered Phase E layout.
- 2026-05-12 | sprint 0008 chunk 1 landed | insta dev-dep + malformed/ fixture scaffold | commit 71bd0f9 | notes: `insta = { workspace = true }` added to `scope-core/Cargo.toml` `[dev-dependencies]` per ambiguity resolution #3; `scope-core/tests/fixtures/malformed/<slug>/` scaffolded for all 7 LanguageId slugs (csharp/go/java/python/ruby/rust/typescript) mirroring the R8 `reference/` layout; per-fixture protocol documented in `malformed/README.md` (hand-crafted synthetic, one failure per fixture, parseable prefix, no secrets; `<case>/` contains broken source + `expected.md` pinning category + rationale + line range + reason tag); per-lang `.gitkeep` markers until chunk 2 lands real fixtures. `cargo check --tests -p scope-core` clean; insta v1.47.2 linked. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2a landed | C# malformed fixtures (5) | commit 02cc266 | notes: 5 hand-crafted synthetic C# fixtures landed at `scope-core/tests/fixtures/malformed/csharp/<case>/` — `truncated-mid-method`, `unbalanced-brace`, `eof-in-string`, `eof-in-verbatim-string`, `eof-in-attribute-list`. Each dir contains `source.cs` (deliberately-broken source, one failure mode per fixture, parseable prefix emits ≥ 1 symbol) + `expected.md` (category + rationale + expected `skipped_ranges` reason tag + expected line range — human-intent contract, snapshot pins precise tree-sitter output in chunk 4). `csharp/.gitkeep` dropped; `malformed/README.md` C# row in per-language category table flipped from `_TBD chunk 2_` to actual dir names. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2b landed | Go malformed fixtures (5) | commit d3b7aad | notes: 5 hand-crafted synthetic Go fixtures landed at `scope-core/tests/fixtures/malformed/go/<case>/` — `truncated-mid-function`, `unbalanced-brace`, `eof-in-raw-string`, `missing-closing-paren-on-call`, `eof-in-import-block`. Same `source.go` + `expected.md` shape as 2a. Raw-string variant exercises backtick recovery surface; import-block variant exercises grouped-import recovery; paren-on-call variant exercises multi-argument call recovery. `go/.gitkeep` dropped; per-language category table flipped for Go row. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2c landed | Java malformed fixtures (5) | commit 2408ed2 | notes: 5 hand-crafted synthetic Java fixtures landed at `scope-core/tests/fixtures/malformed/java/<case>/` — `truncated-mid-method`, `unbalanced-brace`, `eof-in-string`, `eof-in-annotation`, `eof-in-generics-angle`. Same `source.java` + `expected.md` shape. Annotation variant exercises element-value-pair recovery; generics variant exercises the LL(1)-ambiguous `<`/`>` recovery stress point. `java/.gitkeep` dropped; per-language category table flipped for Java row. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2d landed | Python malformed fixtures (5) | commit 3e0d356 | notes: 5 hand-crafted synthetic Python fixtures landed at `scope-core/tests/fixtures/malformed/python/<case>/` — `truncated-mid-function`, `mixed-indent-collapse`, `eof-in-triple-string`, `eof-in-bracketed-call`, `eof-in-decorator`. Same `source.py` + `expected.md` shape. Python has no braces, so mixed-indent fills the "unbalanced delimiters" slot; bracketed-call + decorator variants exercise the implicit-line-continuation recovery surface; triple-string variant exercises the multi-line `"""..."""` recovery. The triple-string fixture leans on the indexer's module-level synthetic `__module__` symbol for the "≥ 1 symbol" floor since the broken string consumes the entire file. `python/.gitkeep` dropped; per-language category table flipped for Python row. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2e landed | Ruby malformed fixtures (5) | commit a9489b9 | notes: 5 hand-crafted synthetic Ruby fixtures landed at `scope-core/tests/fixtures/malformed/ruby/<case>/` — `truncated-mid-method`, `unbalanced-end`, `eof-in-heredoc`, `eof-in-regex`, `eof-in-string-interpolation`. Same `source.rb` + `expected.md` shape. Ruby uses keyword `end` rather than `}` for block closure, and three Ruby-specific string-like surfaces are exercised individually: heredoc bare-identifier delimiter, regex `/.../` ambiguity with division, and string-interpolation `"#{...}"` cascading recovery. `ruby/.gitkeep` dropped; per-language category table flipped for Ruby row. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2f landed | Rust malformed fixtures (5) | commit 9965662 | notes: 5 hand-crafted synthetic Rust fixtures landed at `scope-core/tests/fixtures/malformed/rust/<case>/` — `truncated-mid-function`, `unbalanced-brace`, `eof-in-raw-string`, `eof-in-macro-body`, `eof-in-generics-angle`. Same `source.rs` + `expected.md` shape. Raw-string variant exercises `r#"..."#` recovery; macro-body variant exercises the permissive token-tree recovery surface (`routes! { ... }`); generics variant exercises the LL(1)-ambiguous `<`/`>` recovery stress point. rust-analyzer surface-level diagnostics on the committed fixtures confirm every deliberate syntax error trips recovery (independent verification of hand-crafted-synthetic contract rule 1). `rust/.gitkeep` dropped; per-language category table flipped for Rust row. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 2g landed | TypeScript malformed fixtures (5) — 35-fixture floor complete | commit fad715a | notes: 5 hand-crafted synthetic TypeScript fixtures landed at `scope-core/tests/fixtures/malformed/typescript/<case>/` — `truncated-mid-function`, `unbalanced-brace`, `eof-in-template-literal`, `missing-close-tag-jsx` (TSX, `source.tsx`), `eof-in-type-annotation`. Template-literal variant exercises backtick + `${...}` interpolation recovery; JSX variant is the TS-specific fixture for element-tag recovery (only TS owns the JSX surface among our 7 languages); generics variant exercises the LL(1)-ambiguous `<`/`>` recovery stress point. `typescript/.gitkeep` dropped; per-language category table flipped for TypeScript row — every cell now lists actual dir names, no `_TBD chunk 2_` remain. **35-fixture floor mechanically met (5 × 7 languages); chunk 3 indexer audit + chunk 4 harness follow.** R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 3 landed | indexer skipped_ranges audit | commit 1abe450 | notes: indexer-side wiring audited per `sprints/0008-phase-e-malformed-source-harness.md § Prerequisites`. **Wiring confirmed intact** end-to-end: `scan_tree_sitter_errors` → `LanguageId::plugin_skipped_ranges` → `CodeParser::collect_skipped_ranges` (sort + merge) → `ParsedFile.skipped_ranges` → `file_hashes.skipped_ranges`. Three doc drifts surfaced and fixed in the same commit: (1) `unrecoverable_node` reason-tag listed in ARCHITECTURAL-REFACTOR.md § R6 + sprint 0008 doc + malformed/README.md was aspirational — tree-sitter exposes only `is_error()` + `is_missing()`, so the only emitted reason families are `tree_sitter_error:syntax_error`, `tree_sitter_error:missing_node`, `plugin_skip:<plugin>:<rationale>`. Specs corrected; family-prefix convention documented in malformed README for `expected.md` author guidance. (2) Stale sprint-number ref in `error_scan.rs:12-14` (`sprint 0007` → `sprint 0008`). (3) Stale sprint-number ref in `extract/mod.rs:117` (`sprint 0007` → `sprint 0008`). `cargo check --tests -p scope-core` clean. R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 4 landed | malformed_sources.rs integration harness | commit 826af13 | notes: integration test binary `scope-core/tests/malformed_sources.rs` ships with two `#[test]` fns — `floor_5_fixtures_per_language` (mechanical 5-floor gate) + `every_fixture_satisfies_r6_acceptance` (four-contract loop: no panic, ≥ 1 symbol from parseable prefix, `skipped_ranges` non-empty, `insta` snapshot pins reason + range). Failure accumulates across all fixtures before panicking. 35 `insta` snapshot files committed at `scope-core/tests/snapshots/malformed_sources/<lang>__<case>.snap` — one per fixture, `assert_debug_snapshot!` projection over `(reason, start_line, end_line)` triples. First harness run surfaced 12 reconciliation items: (a) 10 fixtures emitted 0 symbols because tree-sitter recovery sweeps containing class/module/function up into ERROR span — fixed by prepending a small "preamble probe" (complete syntactically valid top-level declaration above the broken content); convention documented in `malformed/README.md § Parseable-prefix preamble`. (b) 2 fixtures emitted empty `skipped_ranges` because the grammar absorbed the original break silently (Python permissive indent; Ruby regex-vs-division ambiguity) — reworked Python with explicit `?? broken_indent_token ??` and Ruby with unterminated regex character class `/welcome[Aa` plus `=~` operator forcing regex context. Snapshot is authoritative for line ranges; `expected.md` prose preserves human-intent failure mode. `cargo test -p scope-core` green (6 suites, 87 tests). All 35 snapshots emit `tree_sitter_error:syntax_error` or `:missing_node`; no `plugin_skip:` entries (no language opts in yet — out of R6 scope). R6 row stays in-progress.
- 2026-05-12 | sprint 0008 chunk 5 landed | Malformed-source CI gate active | commit cd0b0fc | notes: `CI-GATES.md` row "Malformed-source harness" flipped `planned (Phase E)` → `active (sprint 0008 — 2026-05-12)`. New `just test-malformed` recipe wraps `cargo test -p scope-core --test malformed_sources` (matches the script path pinned in the CI-GATES row as authoritative). `gate-refactor` aggregator extended to include `test-malformed`; gate count moves from 14 to 15 (matches the sprint 0008 plan target). `just gate-refactor` verified green end-to-end including the new harness step. R6 row stays in-progress (sprint close in chunk 7 flips R6 → shipped; Phase E stays in-progress until sprint 0009 per renumbered Phase E layout).
- 2026-05-12 | sprint 0008 chunk 6 | codex review checkpoint clean | branch refactor/sprint-0008-malformed-harness | notes: mandatory sprint-close codex review per `sprints/README.md § 9 — Role 1`. Command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base main --title "sprint 0008 — R6"`. Verdict: clean. Codex final report verbatim: "The changes add a malformed-source fixture harness, snapshots, documentation updates, and a dev dependency. The targeted scope-core tests pass, and I did not identify a discrete regression introduced by this patch." Zero P0 / P1 / P2 / P3 findings. Review focus checklist (per `sprints/README.md § 9`): ARCHITECTURAL-REFACTOR.md § R6 acceptance bullets — addressed (codex inspected harness against post-chunk-3-corrected reason-family spec, confirmed no contract drift); CHARTER.md § 3 invariant 5 (tree-sitter resilience) — addressed (35-fixture harness asserts the four R6 contracts); B3 detection — addressed (harness IS the durable detection layer per ARCHITECTURAL-REFACTOR.md table line 73); malformed-fixture set per language — addressed (5-floor × 7 langs); malformed-source CI gate — addressed (flipped active in chunk 5, 14 → 15 gates); REFACTOR-STATUS.md § Stubs outstanding (empty, no stubs introduced or retired this sprint); REFACTOR-STATUS.md § Compat shims outstanding (no shim retired — the five Phase-E-scheduled rows are sprint 0009 scope per renumbered Phase E layout). Full codex output captured locally at `gumiho-mudang-scope/wip/codex-sprint-0008-review.log` (4624 lines — diff + tool calls + verdict; `wip/` is gitignored per the established pattern, log stays local). R6 row stays in-progress; chunk 7 (sprint close) is next.
- 2026-05-12 | R6 | in-progress → shipped | sprint 0008 atomic close | commit 6f22780 | notes: sprint 0008 ships R6 only. Definition-of-done verified: every Deliverables checkbox checked (35 fixtures × `expected.md`, indexer wiring intact, integration test green, snapshot pinning per fixture, CI gate active, snapshot regression surfaces as diff); three pre-Phase-E ambiguities resolved on main pre-branch (commit d99d11c); malformed-source CI gate active in `CI-GATES.md` and CI; snapshot tests pin recorded reason + range per fixture (the mechanical lock for B3 detection); R6 row flipped in-progress → shipped; **Phase E row stays in-progress** (sprint 0009 — charter sweep — closes Phase E + the refactor as a whole per the renumbered Phase E layout); sprint-scope codex review (chunk 6, commit 12a1838) clean with zero P-level findings. Eight chunks landed across the sprint branch (1, 2a–2g fixtures, 3 indexer audit, 4 harness, 5 CI gate, 6 codex review, 7 close); chunk pair commits land on `refactor/sprint-0008-malformed-harness`. Merge to `main` follows per `sprints/0008-phase-e-malformed-source-harness.md § Reporting → Merge`. POST-REFACTOR-PLAN.md eligibility unlocks at sprint 0009's close, not this one.
- 2026-05-12 | sprint 0009 opened | branch refactor/sprint-0009-charter-sweep | notes: final sprint of the refactor. Acceptance-only — no R-move row flips this sprint. Scope per `sprints/0009-phase-e-charter-sweep.md`: chunk 1 closes every row currently in § Compat shims outstanding (6 rows: pre-R0 schema detector, `__module__` synthetic ID fallback, `pub type Edge = RawEdge` alias, `pub use scope_core::{Edge}` re-export, CLI `impact` deprecated alias, refs CLI string aliases); chunk 2 sweeps the full codebase for unrecorded shims via the canonical greps (`legacy` / `deprecated` / `backward` / `compat` / `pre-R[0-9]` / `_deprecated` / `_legacy` / `version: u32` / `schema_version` in stored shapes / `INSERT OR IGNORE` / dual-read patterns / `pub type X = Y` aliases / `pub use foo::Bar` re-exports); chunk 3 wires `just gate-charter` into the aggregator (15 → 16 gates); chunk 4 runs two codex passes (sprint-scope gpt-5.5 medium + full-refactor-scope gpt-5.5 high override against `--base <pre-refactor-baseline>`) and atomically closes the sprint — flipping Phase E `in-progress` → `shipped` and recording the refactor-close log entry in the same commit. POST-REFACTOR-PLAN.md eligibility unlocks the moment that close commit lands on `main`.
- 2026-05-12 | shim:pre-R0-schema-detector | retired | commit b41be2d | notes: sprint 0009 chunk 1 row 1 — `Graph::has_legacy_edges_table` + the early-bail in `Graph::ensure_schema` deleted. Carry-over from sprint 0001 R0 retired because the wipe-policy is now in CHARTER § 3 inv 8 (single-operator posture): the operator deletes pre-R0 `.scope/` indexes by hand, the detector was dead code aimed at a population that does not exist.
- 2026-05-12 | shim:__module__-fallback | retired | commit d8d4574 | notes: sprint 0009 chunk 1 row 2 — `Graph::get_class_relationships` dropped the `<file>::__module__::class` companion ID (class extends/implements are never legitimately module-attributed). `Graph::find_deps` keeps the `<file>::__module__::function` ID in source_ids but threads it through as an `imports_only_id` filter so the SQL surfaces only `kind = 'imports'` from the module synthetic — preserving the genuine import-edge semantic while dropping the pre-fix-index fallback for non-import kinds. `Graph::find_file_deps` keeps the module synthetic as a regular source ID; comment refreshed to drop "backward compatibility with pre-fix indexes". The "split" disposition recorded on the shim row is the implementation here.
- 2026-05-12 | shim:pub-type-Edge-RawEdge-alias | retired | commit 9184302 | notes: sprint 0009 chunk 1 row 3 — `pub type Edge = crate::edge::RawEdge` deleted from `scope-core/src/types.rs`. Six callsites rewritten to `RawEdge`: `FrameworkPlugin::match_edges` trait signature, `Pattern.predicate` fn-pointer type, `dispatch::run_frameworks`, `dispatch::apply_pre_filter`, `parser::CodeParser::extract_edges`, `parser::CodeParser::extract_rust_impl_trait_edges`, `indexer.rs` per-file batch struct, `synthetic_framework` test impl. Four `Edge::builder()` callsites in `scope-core/tests/compile_fail/builder/*.rs` rewritten to `RawEdge::builder()`; trybuild expected stderr snapshots re-recorded.
- 2026-05-12 | shim:pub-use-Edge-re-export | retired | commit 9184302 | notes: sprint 0009 chunk 1 row 4 — `pub use scope_core::{Edge, RawEdge, Symbol}` in `scope-graph/src/graph.rs:15` collapsed to `pub use scope_core::{RawEdge, Symbol}`. The matching `gumiho-mudang-scope/src/lib.rs:35` façade re-export collapsed the same way. `scope-core/src/lib.rs:25` re-export `pub use types::{Edge, Symbol}` collapsed to `pub use types::Symbol`; `RawEdge` already lived on the matching `pub use edge::{…, RawEdge, …}` line. The "post-split crate carve-up" the row notes cited from TODO 0006 § Sprint 0000 is now complete: no caller imports `Edge` via either re-export. Lands together with row 3 in the same commit because the alias removal forced the re-export collapse.
- 2026-05-12 | shim:cli-impact-deprecated-alias | retired | commit 0374a24 | notes: sprint 0009 chunk 1 row 5 — `scope impact` CLI subcommand deleted entirely. `gumiho-mudang-cli/src/commands/impact.rs` + the matching `[[test]]` Cargo.toml entry + the `Impact(commands::impact::ImpactArgs)` clap variant + the match arm in main.rs all removed. Per CHARTER § 2 "Single-operator posture": no user-facing deprecation shim — operator knows `callers --depth N` is canonical. Five integration tests in `tests/integration/test_impact.rs` (and the matching `test_impact__impact_payment_service.snap`) deleted; the depth-N transitive-caller behaviour is covered by `test_refs.rs::test_callers_with_depth_shows_transitive`. Three impact-named tests in other suites migrated: `test_edge_cases.rs::test_impact_depth_limit` → `test_callers_depth_limit`, `test_errors.rs::test_impact_no_index` → `test_callers_no_index`, `test_json.rs::test_impact_json_envelope` → `test_callers_transitive_json_envelope`. Ruby suite migrated similarly: `sc_impact` helper → `sc_callers_transitive`; `test_ruby_impact_process_payment` → `test_ruby_callers_transitive_process_payment` with `command="callers"` assertion.
- 2026-05-12 | shim:refs-callers-impact-aliases | retired | commit 0374a24 | notes: sprint 0009 chunk 1 row 6 — `command_label: &'static str` parameter dropped from `run_callers_transitive` in `gumiho-mudang-cli/src/commands/refs.rs`. JSON envelope hard-codes `command: "callers"`. Visibility tightened from `pub(super)` to `fn` (the only intra-module caller was `run_callers`; the row-5-deleted `impact::run` was the other). Lands together with row 5 in the same commit because the parameter only existed to feed the deprecated `impact` command's JSON envelope. `formatter::ImpactView` doc comment refreshed to drop the `scope impact <symbol>` mention.
- 2026-05-12 | sprint 0009 chunk 1 landed | shim table closed | branch refactor/sprint-0009-charter-sweep | notes: every row in REFACTOR-STATUS.md § Compat shims outstanding struck — table marker prose flipped to `_(Table empty — …)_`. Six row strikes landed via four code commits (rows 1, 2, 3+4 combined, 5+6 combined) followed by this status commit. The "no silent extension" rule (charter): nothing remains to be extended. Chunk 2 (full-codebase grep pass) is next; chunk 3 wires `just gate-charter`; chunk 4 runs two codex passes + closes the sprint. POST-REFACTOR-PLAN.md eligibility unlocks at chunk 4 close.
- 2026-05-12 | sprint 0009 chunk 2 landed | full-codebase grep pass | commits e4bc323, 0924ac7 | notes: chunk 2 manual sweep against the ten canonical greps from `sprints/0009-phase-e-charter-sweep.md § Chunk 2`. Two **unrecorded** shims discovered + retired in this chunk (neither was in the chunk-1 shim table because both were missed by the sprints 0001-0008 audits): (1) `pub mod core { … }` namespace-synth in `gumiho-mudang-scope/src/lib.rs` that re-exported every sub-crate under `gumiho_mudang_scope::core::*` (the pre-split prefix per `docs/todos/0006-split-scope-crate.md`); 28 CLI source files rewritten to the flat namespace via `sed -i '' 's|gumiho_mudang_scope::core::|gumiho_mudang_scope::|g'`, commit e4bc323. (2) Five dead `pub use scope_X as scope_X_crate` façade aliases with zero workspace consumers — forward-looking exports from sprint 0000 that the single-operator codebase never grew a caller for; commit 0924ac7. Loose-grep dispositions (recorded in commit e4bc323 body): `legacy` × 8 (migration notes + language-history terminology, charter-aligned), `deprecated` × 2 (Java `@Deprecated` annotation as language feature, charter-aligned), `backward` / `compat` × N (directional call-graph terms + R10 explicit-null serde decision documentation, charter-aligned), `pre-R[0-9]/pre-fix` × 7 (descriptive prose about historical migrations, charter-aligned), `_deprecated/_legacy` × 0, `version: u32/schema_version` × N (R8 audit-schema wire-format versioning, deliberate by design), `INSERT OR IGNORE` × 1 (single comment about replaced past usage, charter-aligned), `if old_shape / or_else.*as_str` × 0, `pub type X = Y` × 0. **No** escalations were triggered (no shim required a follow-up R-move). Charter sweep gate (chunk 3) takes over as the mechanical successor.
- 2026-05-12 | sprint 0009 chunk 3 landed | just gate-charter wired | commit 64c00a6 | notes: `scripts/gate_charter.sh` ships ten narrow-grep checks targeting the specific shim shapes retired in chunks 1+2: namespace-synth import path (`gumiho_mudang_scope::core::*`), `pub mod core { … }` re-namespace declaration, dead `pub use scope_X as scope_X_crate` façade aliases, `pub type Edge = …` transitional alias, `Edge` import from scope_core/types, `INSERT OR IGNORE` in production SQL, pre-R0 schema-detector fn names (`has_legacy_*` / `pre_r[0-9]+_*`), `command_label: &'static str` deprecation-alias parameter, `scope impact` CLI subcommand wiring, `__module__::class` synthetic-ID fallback in graph query layer. `just gate-charter` recipe added with doc-comment explaining the contract. `gate-refactor` aggregator extended `… test-malformed gate-charter` (15 → 16 gates). `CI-GATES.md § Gate inventory` row appended in `active (sprint 0009 — 2026-05-12)` status with the full check inventory recorded for traceability. The patterns are narrow on purpose — the chunk-2 manual pass cleared loose substring matches (`legacy` / `compat` / `backward` in comments + language terminology); chunk-3 guards specific shim shapes from regressing, not loose patterns. Chunk 4 (two codex passes + sprint close) is next; the sprint close commit also flips Phase E `in-progress` → `shipped`.
- 2026-05-12 | sprint 0009 chunk 4 | codex sprint-scope review round 1 | branch refactor/sprint-0009-charter-sweep | notes: mandatory sprint-close codex review per `sprints/README.md § 9 — Role 1`. Command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base main --title "sprint 0009 — charter sweep"`. Verdict: one P3 finding. Verbatim: "Match direct Edge imports in charter gate — /Users/athena/Projects/Personal/gumiho-mudang/scripts/gate_charter.sh:112-113. When a future shim reintroduces `Edge` via a direct import such as `use scope_core::Edge;` or `use crate::types::Edge;`, these greps will not match because they only handle braced imports; similarly, Check 4 only catches `pub type Edge`, not `pub use ... as Edge`. That lets `just gate-charter` pass despite this check's documented purpose of rejecting any retired `Edge` alias usage." P3 closed in commit 5241587 (round 1 fix): Check 4 broadened to `^pub (type Edge\b|use [^;]*\bas Edge\b)` covering both alias shapes; Check 5 collapsed to single `^use [^;]*\bEdge\b` pattern covering direct, qualified, and braced imports; `\bEdge\b` word boundary precision excludes `RawEdge` / `EdgeKind` / `EdgeBuilder` / `EdgeSummary` / `EdgeId` / `InsertableEdge` automatically. Full codex log captured locally at `gumiho-mudang-scope/wip/codex-sprint-0009-review.log` (wip/ gitignored, log stays local).
- 2026-05-12 | sprint 0009 chunk 4 | codex sprint-scope review round 2 | branch refactor/sprint-0009-charter-sweep | notes: re-run of the sprint-scope review against the post-P3-fix tree. Command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="medium" --base main --title "sprint 0009 — charter sweep (round 2)"`. Verdict: clean. Verbatim final report: "I did not identify any discrete correctness issues in the diff. The changes appear intentional, compile successfully, and the updated command/API surface is reflected in internal call sites and tests." Zero P0 / P1 / P2 / P3 findings. Codex inspected eleven `Edge` mentions surfaced by its own grep (graph.rs:2565/2577 fixture comments, types.rs:5 / edge.rs:3,321 / lib.rs:4 module doc references, extract/{error_scan,mod}.rs schema-field references, frameworks/{dispatch,mod}.rs trait-doc references) and concluded all are charter-aligned prose about the SQL column `Edge.line` / `Edge.args_text` or about the historical type as a concept, not the retired alias. Round-2 log captured at `gumiho-mudang-scope/wip/codex-sprint-0009-review-round2.log`.
- 2026-05-12 | sprint 0009 chunk 4 | codex full-refactor-scope review | branch refactor/sprint-0009-charter-sweep | notes: mandatory full-refactor-scope review per `sprints/0009-phase-e-charter-sweep.md § Chunk 4`. Command: `codex review -c model="gpt-5.5" -c model_reasoning_effort="high" --base 9dc02c7 --title "Refactor close"`. The `model_reasoning_effort="high"` override authorised in `sprints/README.md § 9 — Why these flags`; `--base 9dc02c7` is the pre-refactor baseline (the commit immediately preceding sprint 0000's first commit `bbaaade`). Diff scope = the entire refactor (R0 through R12 + Phase E sweep). Verdict: clean. Verbatim final report: "The patch builds cleanly and the relevant test suites and refactor gates pass. I did not find any discrete, actionable correctness issues introduced by these changes." Zero P0 / P1 / P2 / P3 findings. `git diff --check` flagged one stylistic item: `gumiho-mudang-scope/scope-core/src/types.rs:39: new blank line at EOF` (carryover from chunk 1 row 3 alias deletion); fixed in this close commit by stripping the trailing newline so EOF is a single `\n`. Refactor-close log captured at `gumiho-mudang-scope/wip/codex-refactor-close-review.log`.
- 2026-05-12 | Phase E | unstarted → shipped | sprint 0009 atomic close | commit 7a7a650 | notes: Phase E ships R6 (sprint 0008 — commit 6f22780) + the charter sweep (sprint 0009 — no R-move; ten chunk commits on the sprint branch). Closing the phase closes the refactor as a whole. Acceptance bullets per `sprints/0009-phase-e-charter-sweep.md § Acceptance` all hold simultaneously: (1) Compat shims outstanding empty (chunk 1, six row strikes); (2) Stubs outstanding empty (resolver:phase-a retired sprint 0003 chunk 5); (3) `just gate-charter` clean (chunks 2+3 plus codex round 1 P3 fix); (4) Both codex passes returned no P0/P1 findings; (5) Every R-move R0–R12 and every phase A–E now reads `shipped` in the snapshot tables; (6) Class-3 explicit list (B1, C2, E3) honoured — no other rule delegated to discipline; (7-8) Every active language plugin's `docs/languages/<name>.md` has zero `NEEDS REVIEW` entries; framework-plugin walkthrough is forward-looking (no concrete framework adopted at close); (9) Benchmark `< 10% regression` measurement is a post-merge ops step against `main`; (10) `scope audit confidence` produces parseable `(kind, tier, producer, pattern_id)` precision reports; (11) CI pipeline includes the malformed-source gate (R6), the typed-trait audit (R12), the immutable-source check (R9), and the new charter sweep gate (sprint 0009); (12) Sprints 0001–0008 acceptance bullets each remain demonstrated on `main` (no regression from the sweep); (13) `POST-REFACTOR-PLAN.md` becomes eligible the moment this commit lands on `main`.
- 2026-05-12 | REFACTOR | in-progress → SHIPPED | sprint 0009 atomic close | commit 7a7a650 | notes: the architectural refactor in `ARCHITECTURAL-REFACTOR.md` is complete. Twelve R-moves (R0–R12), five phases (A–E), nine sprints (0001–0009; sprint 0000 was the kickoff that scaffolded the crate split), and one charter sweep. Sixteen active CI gates green via `just gate-refactor` (`ci-edge-sealed`, `test-builder`, `test-typestate`, `ci-context-shape`, `ci-no-fs`, `ci-dispatch`, `ci-trait-shape`, `ci-no-spawn`, `ci-no-network`, `ci-immutable`, `ci-no-framework-scm`, `ci-patterns`, `ci-output-schema`, `audit-confidence`, `test-malformed`, `gate-charter`). Both Phase E codex passes (sprint scope + full-refactor scope) clean. `REFACTOR-STATUS.md § Stubs outstanding` empty; `§ Compat shims outstanding` empty. `POST-REFACTOR-PLAN.md` queue becomes eligible immediately on this commit's merge to `main`; the first post-refactor branch follows its own naming (not `refactor/…`).
