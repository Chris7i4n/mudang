def format_name(name, prefix="", suffix=""):
    return f"{prefix}{name}{suffix}"


def greet(name):
    result = format_name(
        name,
        prefix="Mr.",
        suffix="Esq."


def farewell(name):
    return f"Bye, {name}"
