# `rust` audit-samples — provenance manifest

> **Policy**: [`AUDIT-LABEL-SCHEMA.md` § Corpus accumulation policy](../../../../../../docs/AUDIT-LABEL-SCHEMA.md#corpus-accumulation-policy).

Append-only. One row per committed `*.jsonl` sample in this directory. A labelling pass that adds a sample appends one row; existing rows are never edited.

| `sample_file` | `labeller_id` | `labelled_at` | `scope_commit` | `sample_count` |
|---|---|---|---|---|
| _(none yet)_ | — | — | — | — |
