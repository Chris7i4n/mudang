//! gumiho-mudang CLI — binary `mudang`.
//!
//! Orchestrates the syntactic engine (`gumiho-mudang-scope`) and the
//! semantic oracle (`gumiho-mudang-lsp`), routing each query to the
//! appropriate divination technique. User-facing commands, output
//! formatting, cache invalidation, and routing rules live here.
//!
//! Pending migration from legacy `scope` CLI. Skeleton only.

fn main() {
    println!("mudang: skeleton — migration pending");
}
