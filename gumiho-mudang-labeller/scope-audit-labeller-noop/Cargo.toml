[package]
name = "scope-audit-labeller-noop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
publish.workspace = true
description = "Reference no-op Scope audit labeller. Stamps labeller_id and passes every record through unchanged. Proves the Labeller trait + JSONL IO loop end-to-end."

[[bin]]
name = "scope-audit-labeller-noop"
path = "src/main.rs"

[dependencies]
scope-audit-labeller-core = { path = "../scope-audit-labeller-core" }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }
